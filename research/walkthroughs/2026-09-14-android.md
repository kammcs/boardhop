# Android walkthrough — Pixel 10 Pro and Pixel Tablet emulators, 2026-09-14

**Build:** debug APK at `e1566c1` (HEAD after the Mac session's day: search S1–S3,
mentions M-A–M-D, work item tabs, comment attachments T-A–T-C, the glass-shell keyboard fix),
`--dart-define-from-file=.env`, installed on `boardhop_pixel_10_pro` (1280×2856) and
`boardhop_pixel_tablet` (2560×1600). Signed in as kkamm@cloudcover.it, puremedia, scratch project.
`flutter analyze` clean, 885 tests green before the pass. Screenshots in `.shots/` (gitignored):
`01`–`53` phone, `t01`–`t11` tablet.

Every feature below had been accepted on the iOS simulators only (`2026-09-14-search.md`,
`-mentions.md`, `-attachments.md`); the mentions and attachments walkthroughs list Android/Gboard
and the camera path as "blocked, not exercised". This pass closes those.

## Search (NEXT-STEPS 20) — pass

- Home magnifier → the search page with the field focused and the Project | All pill; "Searching
  Work items…" placeholders while the debounce ran; Code answered its `infoCode 15` line for the
  scratch repository in the page's own words (`08`, `09`).
- Project scope for "board": Work items · 3 with the term bold, Pull requests · 1 (8334). All
  projects: 42 work items with project names and highlighted lines, See all with the count line,
  "cached · just now" and the sort icon (`10`, `11`). A work item hit opened its detail; Back kept
  the results (`12`, `13`).
- "scratch" found `azure-pipelines.yml` (3 matches) once the index answered, and PR 8334 (`41`).
- Work tab inline filter: "15545" under Assigned to me correctly matched nothing (it is
  unassigned), matched under Recently updated (`15`, `16`).
- Tablet: the page at the expanded width with the labelled Project / All pill and the rail (`t08`).
- Light theme on the phone (`53`).

**Observation:** the See-all page did not show the Type / State chips on this cached first open
(`11`); the iPhone walkthrough shows them. Not investigated — possibly the facets arrive only with a
fresh fetch. Worth one look on a device with the network fast enough to see the fresh page.

## Mentions (NEXT-STEPS 21) — pass, Gboard included

- `@kel` in the #15545 Discussion composer: the floating list above the field, "Kelly Kamm ·
  On this item", the typed prefix bold, Gboard's suggestion strip up at the same time (`24`).
  Picked → a semibold link-blue run `@Kelly Kamm` in the field (`25`).
- `#1554` → five work items with type icons; `!83` → the two scratch PRs "On this pull request"
  (`28`, `29`, `30`). Picked both.
- Posted with an image (below): the comment read back with all three runs styled and #15546
  tappable — it opened the work item with its own badges Related 2 / Comments 4 (`38`, `39`).
- Same on the PR page composer: `@kel` list, pick, text, post; the new thread renders the mention
  (`46`, `47`, `51`).
- Existing mentions from the iOS runs render blue on Android; the w30 spike's raw
  `<a data-vss-mention>` comment shows as literal text, which is that spike's own probe, not a
  rendering defect.
- Gboard composing: `adb shell input text` types through the IME with predictions on; the field
  never cleared and the list never closed. The keyboard-under-a-real-thumb case (autocorrect
  replacing a partial name) is still only covered by `mention_controller_test.dart`.

## Work item detail in three tabs (NEXT-STEPS 22) — pass

- #15558: Details / Related 1 / Comments (no badge for zero) (`13`); #15545: Comments 19 (`17`);
  #15546: Related 2 (children list), Comments 4 (`39`, `40`).
- Comments tab owns the composer as the bottom bar; the glass dock disappears under the keyboard,
  the composer rides above it (`24`).
- Tablet list + detail pane: the strip lives in the pane's own bar, Details first, Comments with
  the composer only on that tab (`t10`, `t11`). A real exchange with Alex Bondurant is in #15546's
  comments — a puremedia tester is on build 11.

## Attachments in comments (NEXT-STEPS 23) — pass, camera path included

- Read side: #15545's T-B/T-C comments render their inline images (320 pt cap) and file links
  (`tb-notes.txt`, `w32-probe.txt`); the viewer opens from a tap with the stamped file name in the
  bar (`18`, `21`, `22`). Images fetch slowly on the emulator (several seconds each; spinners
  meanwhile).
- Work item comment with an image from the **system photo picker** (Android 16 picker, "Boardhop
  will only have access to the photos you select"): chip with thumbnail, name and 1.2 MB, Send
  showed the spinner on the chip and the button, upload took ~25 s on the emulator, the comment
  read back with the image inline and the badge 19 → 20 (`33`–`38`).
- PR page comment with a photo from the **camera** (the emulator's virtual scene): confirm screen,
  chip `photo-…jpg · 34 KB` (shrunk from the sensor size), Send, the new thread with the mention and
  the image, Comments 7 → 8 (`48`–`51`).
- PR thread images from the iOS runs (T-B reply, T-C diff-line thread) render inline (`44`, `51`).
- Light theme on the PR comments (`52`).

**Observation:** with the keyboard down, the Add attachment sheet sits lower than with it up, so a
scripted tap meant for "Choose from library" landed on "Take photo". Not a defect; it is why the
camera path got exercised.

## Keyboard (NEXT-STEPS 21 "keyboard gap") — pass

Dragging the PR comment list with the composer focused dismissed Gboard and kept the typed text and
the mention run (`47`).

## Relay

Both comments reached the relay as `wi.updated` / `pr.comment` for the scratch project and were
dropped as self-mentions by the actor rule (candidates > 0, recipients 0), as designed.

## Not exercised

- Offline (T6, search §5): the emulator's network was not cut.
- Files (non-image) from the Android document picker: the file path was proven on iOS; only the
  photo picker and the camera were used here.
- Text size and the 3+ notification group summary: unchanged from yesterday's notes.
- The first call after a cold start still fails once (the org list came up "No organizations found"
  and pull-to-refresh fixed it): the pre-existing transient-network issue from NEXT-STEPS 19, not
  new.
