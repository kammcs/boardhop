<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Local notifications for new activity (Android 13+ runtime prompt). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Work item attachments (phase 5). image_picker hands the capture to
         the camera app and reads photos through the system photo picker, so
         no CAMERA or READ_MEDIA_* permission is needed; file_picker uses the
         Storage Access Framework, which needs none either. The feature is
         declared optional so the app still installs on a device without a
         camera (a Pixel Tablet has none). -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />

    <application
        android:label="Boardhop"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- MSAL redirect target for browser / Authenticator sign-in.
             Path is the base64 signature hash from android/secret.properties. -->
        <activity
            android:name="com.microsoft.identity.client.BrowserTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="com.kammcs.boardhop"
                    android:path="/${msalSignatureHash}"
                    android:scheme="msauth" />
            </intent-filter>
        </activity>

        <!-- With the Installations registration below in force the SDK sends
             FCM_REGISTERED instead of NEW_TOKEN, and firebase_messaging's
             service only handles NEW_TOKEN, so the token never reaches Dart.
             BoardhopMessagingService is that service plus onRegistered. -->
        <service
            android:name="io.flutter.plugins.firebase.messaging.FlutterFirebaseMessagingService"
            tools:node="remove" />
        <service
            android:name=".BoardhopMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- No FCM registration until the user turns notifications on: the
             app calls setAutoInitEnabled(true) from PushService.refreshToken().
             It also wins a race — with auto-init on, the SDK emits onNewToken
             during the ContentProvider start, before any Dart listener exists,
             and the token is never seen again. -->
        <meta-data
            android:name="firebase_messaging_auto_init_enabled"
            android:value="false" />

        <!-- Opt in to FCM's Firebase-Installations registration. Without it
             GmsRegistrationClient.isV1RegistrationEnabled() is false and the
             SDK falls back to the legacy IID path, which answers
             "FCM Registration failed!" on this Play services build. -->
        <meta-data
            android:name="firebase_messaging_installation_id_enabled"
            android:value="true" />

        <!-- Pushed notifications land in the same channel as the polled ones
             (created at startup by NotificationService), so they look alike
             and share the user's per-channel settings. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="activity" />

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
