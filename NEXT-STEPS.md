# Boardhop — state and next steps

**As of:** 2026-09-11, first iOS build and full walkthrough on the iPhone 17 and iPad Pro simulators (step 16); milestones 1–3 and the repos plan done on Android.
**Read first:** [research/00-feasibility-summary.md](research/00-feasibility-summary.md) section 0 (decisions) and 6a (spike results), then [research/08-stack-comparison.md](research/08-stack-comparison.md).

## Where things stand

| Area | State |
|---|---|
| Product | Boardhop: Flutter mobile client for Azure DevOps Services, iOS + Android, phones + tablets. Jira-style boards and work items, GitHub-style PR review. |
| Stack | **Flutter**, locked 2026-09-10. `msal_auth` for Entra sign-in with the Authenticator broker; `drift` for offline cache and write queue; `html_editor_enhanced` for rich text (chosen by spike F3); `flutter_widget_from_html` and `flutter_markdown_plus` for rendering; `diff_match_patch` + `re_highlight` + custom diff viewer. |
| Scope | Entra OAuth only at launch (no PAT). Cloud only. Read plus lightweight writes in v1. Responsive tablet layout, multi-pane later. Free app; revenue via a Marketplace extension plus a relay in the customer's Azure tenancy. Build order: work items and boards, then PR review, then pipelines. |
| Research | Documents 01–09 in `research/`, all committed. Spike scripts and consolidated results in `research/spikes/` (raw result files are gitignored: client data). |
| Spikes done | Org-level PR list exists; `multilineFieldsFormat` returned at 7.1 without a `fields` filter; WEF column write derives State; `validateOnly` gives field-level errors; stale rev → 412; line comments track across pushes when read with `$iteration`/`$baseIteration`; ```` ```suggestion ```` fence renders as an applyable suggestion; poll cycle ≈ 0.013 TSTU; profile/accounts APIs on `app.vssps` are Entra-only. |
| Entra | kammcs tenant exists (from the Multipass code-signing project). **Boardhop registration created** as multi-tenant public client with Azure DevOps delegated permissions (`vso.threads_full` is not offered in Entra; `vso.code_write` covers PR threads). **Puremedia admin consent granted** for tenant `342d4cd1-7ea8-4452-8ceb-542b71d159f6` (verified domain `cloudcover.it`); the trailing browser error was the redirect to the `msauth` URI, not a failure. |
| Test org | `https://dev.azure.com/puremedia`, project "CloudCover 2.0" for read tests; scratch project **"DevOps Mobile App"** for every write (work items #15503–#15507 and #15539–#15541 from the form spikes; PR 8319 completed 2026-09-11, scratch PR **8334** seeded by `w09` with a line comment and a suggestion; pipeline **boardhop-scratch** (id 139) on the hosted `Azure Pipelines` queue with environment `boardhop-scratch` (id 18) behind an approval check, Kelly as approver). PAT in the AzureDevOps MCP config is authorized for spikes; it reads the Build API but cannot create agent queues. |
| Repo | `git@github.com:kammcs/boardhop.git`, branch `main`. Commit and push are pre-approved. |
| Emulator | AVD `boardhop_pixel_10_pro` (Android 17, Play image). Start with `tool/start-emulator.ps1`; the `-dns-server` flag is required on this host. |
| iOS | Built and signed in on the Mac 2026-09-11 (Xcode 26.6, iOS 26.5 simulators, SwiftPM, no Podfile). `tool/shot-ios.sh` drives the simulators through idb. Still untested: physical device, Authenticator broker, Conditional Access. |
| Plugin fork | `packages/msal_auth` is a vendored, patched copy of msal_auth 3.5.3 (path dependency). Keep the diff small; see its README. |
| Kanban board | Spike F4 done 2026-09-10: no package. `lib/features/diagnostics/board_probe/hand_rolled_board.dart` (Flutter `LongPressDraggable` + `DragTarget`, auto-scroll both axes, insertion gap, haptics, drag session owned by a `Listener`) is the seed for the real board; measured 2.1% janky drag frames at 200 and 1,000 cards in a profile build on the emulator. |
| Diff viewer | Spike F5 done 2026-09-10: `lib/features/diagnostics/diff_probe/` holds the seed (Myers `LineDiff`, `CodeHighlighter` over `re_highlight`, `PrDiffSource` for iterations/changes/items/threads, row widgets on `super_sliver_list`). |
| Pull requests | Milestone 2 done 2026-09-10 except Complete/Abandon (untested): inbox with offline cache, detail with Checks, iteration picker, file diffs with tracked threads, replies and thread status, suggestion apply (commits through the Pushes API), vote. |
| Pipelines and activity | Milestone 3 first pass 2026-09-10: `lib/features/pipelines/` (runs, definitions with Run, approvals, run timeline with polling, logs; writes untested until the scratch project has a pipeline), `lib/features/activity/` (polled feed with a last-seen marker) and local notifications (`lib/core/notifications/`, `lib/data/activity_sync.dart`). |
| Design system | Done 2026-09-10: `BoardhopTheme` light/dark from one seed, `BoardhopColors` extension for work item / PR / pipeline / diff colors, tokens, breakpoints, `ThemeController` (system default, persisted; Settings > Appearance). Rules in DESIGN.md. |
| App scaffold | Done. Flutter 3.47.3 / Dart 3.13. `lib/core` (config, `AdoClient`, rate limits, typed errors), `lib/auth` (`AuthService` over `msal_auth` 3.5.3, `AuthBloc`), `lib/data` (drift: organizations, projects, pending_writes), `lib/features/*` (sign-in, org picker, project list, diagnostics; placeholders for work items, boards, PRs, pipelines, activity). Android: `assets/msal_config.json`, `BrowserTabActivity` with the hash from gitignored `android/secret.properties`. iOS: URL scheme, `LSApplicationQueriesSchemes`, keychain group entitlement, deployment target 16.0. 13 unit tests pass; `flutter analyze` is clean. |
| Still administrative | Partner Center + MPN ID for publisher verification; Visual Studio Marketplace publisher; Apple and Google developer accounts; privacy policy; domain for boardhop. |

## Values needed from Kelly

- ~~Entra client ID~~ Provided in the gitignored `.env` as `BOARDHOP_CLIENT_ID`; run with `--dart-define-from-file=.env`.
- Confirmation that `msauth.com.kammcs.boardhop://auth` and the Android debug redirect `msauth://com.kammcs.boardhop/%2F%2Fksb0DQrePXmmxPydZ%2FUbpze98%3D` are saved on the registration.
- A physical device with Microsoft Authenticator for the broker path and Conditional Access (the emulator run used the browser fallback).

## Next steps, in order

1. ~~**Scaffold the Flutter app**~~ Done (see the App scaffold row above). CI: not set up. A GitHub Actions workflow was added and removed the same evening (2026-09-10) because Actions minutes are a budget item; Kelly decides if and where CI runs. `flutter analyze --fatal-infos` and `flutter test` pass on a clean clone (analysis excludes `packages/**` and `research/**`, which carry their own pubspecs).
2. ~~**Spike F1**~~ Passed 2026-09-10 on a Pixel 10 Pro / Android 17 emulator (`tool/start-emulator.ps1`, then `flutter run -d emulator-5554 --dart-define-from-file=.env`): browser-fallback sign-in, silent restore after force-stop, org discovery, 2 KB token with ten `vso.*` scopes, all diagnostics green. See `research/spikes/results/README.md`. Still open: the Authenticator broker path and iOS, which need a physical device.
3. ~~**Spike F2**~~ Done 2026-09-10. `msal_auth` is vendored at `packages/msal_auth` (3.5.3+boardhop.1) with `claims`, `forceRefresh` and `clientCapabilities` added; CP1 is declared; `AdoClient` retries a 401 once via `AuthService.resolveChallenge`. Emulator run: forceRefresh and claims requests both reach native MSAL. Token lifetime stays ~75 min with CP1 (no long-lived CAE tokens from Azure DevOps). Left open: trigger a real revocation to see an `insufficient_claims` 401 end to end; compile the Swift side on a Mac; offer the patch upstream.
4. ~~**Spike F3**~~ Done 2026-09-10: `html_editor_enhanced` (WebView) is the editor; Delta-based editors destroy tables and mentions on real content. See research/spikes/results/README.md. Production notes: set content via `evaluateJavascript` with a JSON-encoded string, keep the editor's slot in its parent stable, load attachment images with an auth header, and watch the `flutter_inappwebview` beta override until 6.2 ships stable.
5. ~~**Spike F4**~~ Done 2026-09-10: no package. `drag_and_drop_lists`, `appflowy_board` and `boardview` all jank far more than Flutter's own `LongPressDraggable` + `DragTarget` on the same emulator battery (2.1% janky drag frames vs 15–25%), and each carries dependency risk; `super_drag_and_drop` would need a Rust toolchain everywhere. The hand-rolled board in `lib/features/diagnostics/board_probe/` (edge auto-scroll on both axes, insertion gap, haptics, 1,000 cards fine) is the seed for the real board. See research/spikes/results/README.md for the production notes, above all: own the drag session in a `Listener`, since a `Draggable` unmounted by auto-scroll never reports drag end.
6. ~~**Spike F5**~~ Done 2026-09-10: own Myers line diff (`LineDiff`; `diff_match_patch` only for intra-line emphasis), `re_highlight` whole-file highlighting split per line, `super_sliver_list` rows (exact jump-to-line in 101 ms; `ListView.builder` needs 902 ms and misses by 50 lines), tap-to-comment gutter, threads overlaid from `$iteration`/`$baseIteration` (verified on PR 8319: both w03 threads land on their tracked lines). See research/spikes/results/README.md. Production notes: highlight off the UI thread (376 ms for two 3,000-line files), collapse unchanged regions, cache content by SHA, composer write path with `changeTrackingId`.
7. **First milestone (in progress, 2026-09-10).** Done: project shell with bottom navigation / rail (Work items · Board · Pipelines), "assigned to me" list (WIQL `@Me` → `workitemsbatch`, cached in drift under a list key), work item detail (full read without a `fields` filter so `multilineFieldsFormat` is honoured; HTML through `flutter_widget_from_html_core` with bearer-authenticated attachment images, Markdown through `flutter_markdown_plus`; discussion from the preview Comments API rendered from `renderedText`), Kanban board (board definition + team area paths + WIQL ordered by StackRank/BacklogPriority with fallback, cards distributed by the WEF column field, drag-and-drop on the shared `KanbanBoard` widget, optimistic column move written as WEF column + mapped state with `test /rev`, reverted with the message on failure). Types and state colors come from `workitemtypes` and are tinted for dark mode. Also done: comment posting (`format=markdown`), state picker from the type's states, assign to me / unassign, all rev-guarded with rule errors shown per field (verified on scratch task 15503). Board, second pass: split columns render as Doing/Done slots (WIP count over the whole column), swimlanes as a filter chip row with a lane badge on cards, and every drop writes the rank through `workitemsorder` (the moved card plus its run of unranked neighbours, bounded by ranked cards; verified on the scratch board, ranks assigned to both stories). Split columns and lanes were verified on the emulator 2026-09-11 after Kelly became team and project administrator of the scratch project and `research/spikes/w05_board_config.py` configured its board (Active split into Doing/Done with WIP 5, an "Expedite" swimlane holding 15507): the lane chips, the Doing/Done halves and the 1 / 5 count render as designed. Work items tab has a list chooser: assigned to me, recently updated (7 days), or any saved query from the first two levels of My/Shared Queries (flat, tree and one-hop results handled), each cached under its own list key. Title and description editing through the F3 editor (HTML) or a Markdown field, only changed fields written. Offline queue: a network failure on a move, field edit, title/description edit or comment queues the write in the `pending_writes` table, applies it to the local cache, and a banner in the project shell shows what waits; the queue drains on open, on app resume and on Retry, replaying each write with its original `test /rev` so a 412 becomes a reviewable conflict instead of an overwrite (verified on the emulator with the radio off). Tablet: at medium and expanded widths the Work items tab shows the list beside the selected item (rail instead of bottom bar). Milestone 1 scope is complete.
8. **Milestone 2: pull requests (first pass done 2026-09-10).** Inbox (org-wide via the S4 endpoint, or per project as a shell tab) with To review / Created by me / All active; identity from `connectionData` (needs `api-version=7.1-preview`). Detail: Overview (Markdown description, reviewers with votes, linked work items), Files of the latest iteration, Conversation with a composer; vote menu, Complete and Abandon dialogs. File diff on the F5 engine (now in `lib/features/pull_requests/diff`) with threads read at `$iteration`/`$baseIteration` and a gutter composer that posts an anchored thread with `changeTrackingId`. Verified on scratch PR 8319: vote to Approved and back, line comment, conversation comment; CloudCover PRs read-only. Second pass (2026-09-10): a shared `ThreadCard` under diff lines and on the Conversation tab with inline replies (`POST threads/{id}/comments`, parented on comment 1) and a status menu (Resolve / Won't fix / Close / Reactivate via `PATCH threads/{id}`); Checks section on the overview from `mergeStatus`, `policy/evaluations` (7.1-preview.1, artifact id per spike s15) and the newest iteration's `statuses`, folded into one `PrCheck` vocabulary and tolerant of either read failing; iteration picker on the Files tab and in the diff app bar (changes and threads re-read for the chosen iteration, threads land on their iteration-1 lines); PR lists cached as JSON blobs in the `CacheEntries` table (schema v3) so the inbox opens offline and keeps the cached list with its age when the network read fails. Verified on the emulator: reply and resolve on the diff page, reply / won't fix / reactivate on the Conversation tab (PR 8319), Checks on a CloudCover PR (read-only), cached inbox with the radios off. Suggestion apply (2026-09-10 evening): a ```suggestion fence in a file thread shows "Apply suggestion" on the latest iteration of an active PR; it replaces the anchored lines (`rightFileStart..rightFileEnd`, line-ending style kept), commits through `POST repositories/{id}/pushes` with `refUpdates[].oldObjectId = lastMergeSourceCommit`, resolves the thread and reloads at the new iteration. Verified on PR 8319 (iteration 3 created from the app). Complete verified 2026-09-11 on PR 8319 (merged, source branch deleted; the page keeps reloading until the queued merge finishes). Abandon verified the same day on scratch PR 8334, which was then reactivated through the API for future tests. Not yet exercised: required-reviewer names in the Checks section. Then milestone 3: pipelines and the activity feed. Dogfood against puremedia (writes only in "DevOps Mobile App").
9. **Milestone 3: pipelines (first pass 2026-09-10).** `PipelineRepository` over the Build API (definitions with latest builds, recent runs newest first, run, timeline, log lines as a JSON string array), the Pipelines API for queueing (`POST pipelines/{id}/runs` with `resources.repositories.self.refName`), cancel (`status: cancelling`), retry stage (`PATCH builds/{id}/stages/{identifier}` with `state: retry`), and YAML approvals (`pipelines/approvals?state=pending&$expand=steps`, `PATCH` with `[{approvalId, status, comment}]`). Definitions and runs are cached like PR lists. Pipelines tab: Runs with a pipeline filter, Pipelines with a Run action (branch prompt), Approvals with approve/reject. Run page: header, stages → jobs → tasks (Phase wrapper flattened, siblings by `order`, classic builds with tasks under the root job), issues inline, polls every 8 s while active, Cancel / Run again / Retry stage. Log page with `##[error]`/`##[warning]`/`##[section]` accents, timestamp and wrap toggles. Verified read-only on CloudCover 2.0 (108 definitions, run list, a failed run's timeline and log); the Entra token covers `vso.build`. Writes verified 2026-09-11 on the scratch pipeline (created by `research/spikes/w06_scratch_pipeline.py` + `w08`): queue from the Pipelines tab, cancel mid-run (issues shown inline), retry the canceled stage (attempt 2), environment approval from the Approvals tab, Deploy stage ran after it. Lessons: a run rejected before an agent starts (pool not authorized) completes as failed with `validationResults` and no timeline, now shown on the run page; a pipeline's first use of an environment waits on `Checkpoint.Authorization` and the approval object does not exist until the pipeline is permitted for the environment (the run page says "waiting for resource authorization"); the approvals service returns ids as strings. Activity feed done the same day: `ActivityRepository` merges PRs to review, my PRs, one org-wide WIQL (assigned to me, changed in 14 days) plus a batch, and the last two days of builds from up to five "pinned" projects (those whose Pipelines tab was opened, found through the `pipelines:runs:` cache keys); cached per org with a last-seen marker; `ActivityPage` (bell icon on the project list) refreshes on open, pull, resume and every 60 s, filters by kind, shows a dot on new items. Verified on the emulator against puremedia. Local notifications done the same evening: `NotificationService` (flutter_local_notifications 22, opt-in in Settings > Notifications or the feed banner, Android 13 prompt, taps and cold-start launches routed in the app) and `ActivitySync` (polls the last-opened org every 3 minutes in the foreground and on resume, announces items newer than the last feed visit that were not announced before and not the user's own change, first run only sets the baseline, paused while the feed is open). Verified on the emulator: permission prompt, a "Build succeeded" notification after resume, tap opened the run. Android needed `POST_NOTIFICATIONS` and core library desugaring. Log tailing dropped 2026-09-11 after spikes s20/s21/w12/w13: the agent uploads a task's log only when the task finishes, even for 18 KB of output (the timeline record's `log` is null until then), so REST cannot follow live output and only the web's undocumented SignalR feed could; the run page's timeline polling already makes a task's log tappable as soon as it completes. Refresh buttons were removed from the pipelines, run, pull request and activity pages, which all pull to refresh. Classic release approvals deferred: spike s19 found five old classic definitions in CloudCover 2.0 and zero pending approvals anywhere in puremedia. Still to do: signed-URL log download for huge logs, iOS notification settings in `Info.plist` review. Tablet-width pass over the repos screens done 2026-09-11 (Home, Work, commits, commit page, branch picker, code browser two-pane): no layout defects.
10. **Style pass (2026-09-11).** People: `IdentityRef` keeps the Graph descriptor and `AvatarStore` reads `vssps …/graph/Subjects/{descriptor}/avatars` (base64 PNG; the `MemberAvatars` and `identityImage` links on identities refuse the Entra token), cached in memory and on disk under app support for 7 days, shown by `IdentityAvatar` over the initials. Organization page: `AccountHeaderBar` with the photo, email and company from Microsoft Graph (`User.Read` acquired silently, falls back to the Azure DevOps profile), cached. Organization and project tiles match the web (spike s16): projects use the service's own algorithm (.NET string hash into a 12-color palette, first-and-last-word initials with digits and punctuation dropped) drawn by `AdoTiles`, and show the project picture when the default team's Graph avatar is not `isAutoGenerated` (descriptor cached in the projects table, schema v5); organizations use the azure-devops-ui Coin scheme because the service has no image for them (the web's org rail may pick a different color). A project with a picture was verified 2026-09-11 after `w10` uploaded one to the scratch project: the tile shows the picture on the next app start (avatar misses are remembered for the session only).
11. **Multiple accounts (2026-09-11, Kelly's ask: kammcs and puremedia under different sign-ins).** MSAL runs in multiple-account mode (`account_mode: MULTIPLE`, `MultipleAccountPca`); `AuthService` keys tokens by account and tenant, `AuthBloc` holds the account list and signs accounts in (account picker) and out one at a time. Decisions with Kelly: an organization can appear under every account that reaches it (a person may hold an admin and a regular identity in the same org), so every org route is account-scoped (`/a/{account}/orgs/{org}/…`, `Routes`), the `/a/:account` shell route binds the repositories to that account (`AccountDeps`: client bound to the account, `JsonCache` namespaced for PR lists, pipelines, the activity feed and the account header, work item list keys prefixed, the write queue tagged with the account), background sync watches one org per account, and signing an account out wipes its organizations, caches and queued writes while keeping what another account also reaches. Organizations table is keyed by (account, name) (schema v6, recreated). The organization page shows a header row per account (photo, email, company, sign-out on the right) above that account's orgs, with "Add account" in the app bar. Verified on the emulator with one account; the second sign-in (kammcs) is Kelly's to type.
12. **Branding (2026-09-11).** Kelly's `assets/brand/logo.png` and `splash.png` (1254 px masters, logo baked into the splash's center). iOS: `LaunchScreen.storyboard` shows the splash as a full-screen `scaleAspectFill` image (`LaunchBackground` imageset), so the logo stays centered on any device or orientation. Android 12+: the system splash can only show a color plus an icon, so `values-v31/styles.xml` uses the artwork's edge color and an adaptive icon cut from the splash (`drawable-v31/splash_icon.xml`); older Android centers the artwork on that color (`launch_background.xml`, bitmap drawables cannot cover-scale). Flutter's `/` route is `SplashPage`, the same image with `BoxFit.cover`, so every platform lands on the identical look before sign-in state is known. Launcher icons come from `dart run flutter_launcher_icons` (config in pubspec: logo as the flat icon, splash artwork as the adaptive foreground). A larger master (2500 px or more) would keep the logo crisp on phones, where cover scaling enlarges it about 2.3x.
13. **Repos and code browsing (plan agreed with Kelly 2026-09-11; in progress).** Research and plan in `research/10-repos-and-code-browsing.md`: four-tab shell (Home / Work with Items·Board selector / Repos / Pipelines), repository list with language metrics and Azure DevOps favorites, repository page (PRs, branch row, Code, Commits, README), code browser and highlighted file viewer, branch picker with stats, commits and per-file diffs on the existing engine, code search. Decisions: Home = project overview, repo rows show language · branch · size, PRs open the existing list filtered by repo, Azure DevOps favorites, disabled repos badged at the bottom, branch remembered per repo, plus a tags page, share links and file editing (re_editor, <200 KB, commit to a new branch then offer a PR). Spike s17 verified every endpoint read-only; w11 (favorite write) runs on the scratch repo before phase 1. Order: shell first. Phase 0 (four-tab shell) and phase 1 (repository list, repository page with branch picker, README and PR link, favorites) landed 2026-09-11 and were verified on the emulator against CloudCover 2.0 and the scratch project. **Kelly:** add `vso.analytics` and `vso.profile_write` to the Boardhop registration and re-grant admin consent in puremedia (research/09 §A4) so the language line and the star work. Phase 2 (2026-09-11): code browser (`CodeBrowserPage`, one `OneLevel` listing per folder, folders first, breadcrumb, branch switch, long-press sheet with History / Copy path / Copy link / Open in browser) and file viewer (`FilePage`: metadata read, `blobs/{sha}` size gate at 1 MB with "Load anyway", images through `$format=octetStream` with the bearer token, binaries as a size-and-type card, text highlighted on a background isolate through `CodeHighlighter.highlightLinesAsync` with plain rows shown first, line numbers, wrap and text size, Markdown files rendered with a source toggle, the last 20 files cached by object id for offline reopening). README relative images and links now resolve through `RepoMarkdown`. Verified on the emulator against MonitorIT (CloudCover 2.0): folders, breadcrumb, Markdown preview and source, wrap, TypeScript and SVG-as-XML highlighting, a PNG, the action sheet. Not yet exercised: the large-file gate, a non-image binary, offline reopen, a cold deep link (`_RepoRoute` falls back to the network). Favorites toast (Kelly's ask, same day): starring or unstarring shows "{repo} added/removed from favorites" with a Show action that scrolls to the row's new section (the list moved onto `SuperListView` so an unbuilt row can be scrolled to by index) and tints it briefly; verified on the scratch repo. Phase 3 (2026-09-11 evening): commits list (`CommitsPage`, branch or path history, 50 per page with more on scroll, first page cached), commit page (`CommitPage`: message, author, parent chips, linked work item chips from `commitsbatch`, changed files with A/M/D/R badges, menu with browse-at-commit, copy id, copy link, open in browser), file diff between any two refs on the PR diff engine (`FileDiffPage`, highlighting on the isolate), branch compare (`ComparePage`: ahead/behind, common ancestor, changed files each opening a diff, reached from the picker's compare icon and the commits page), and a Tags tab in the branch picker (peeled to their commit, open the commit or browse files at the tag). `GitVersion` maps a ref string to branch, tag or commit version descriptors so the code browser and file viewer work at any of them. Verified on MonitorIT: list, commit page, edit diff against the parent, compare hyperv branch vs main (17 ahead / 4 behind, 30 files), tags list. Lesson: `commitsbatch` returns no `parents`; the single commit read does, so the page merges both. Phase 4 (2026-09-11, late evening): Home tab content (`ProjectHomePage`: pinned repositories from the Azure DevOps favorites, or recents when none; pull requests to review and of mine in the project; work items assigned to me; latest five runs, each section cached-first and failing on its own); code search (`CodeSearchPage` over `almsearch …/codesearchresults`, project-wide from the Repos app bar and the Home tab, repository-scoped from the repo page, results grouped by repository with match counts, paging on scroll, `infoCode` mapped to plain messages, 404 explained as the extension missing); a hit opens the file viewer with `find=`, which tints and scrolls to the first line containing the term since the service returns no line numbers; code browser shows the selected file in a right pane at medium and expanded widths (`FilePage(embedded: true)`). Verified on the emulator: Home sections against CloudCover 2.0, search for TODO (408 hits), a hit landing on its line, and the two-pane browser at 2000x1280. Phase 5 (2026-09-11, night): `FileEditPage` on `re_editor` 0.10 (same grammars as the diff highlighter, line numbers, dirty tracking, discard confirmation) edits text files under 200 KB from the file viewer's pencil; the commit sheet proposes `alias/stem-MMdd-HHmm` (sanitized for Git), the ref is created at the tip the app read and the change pushed to it (a push cannot create the ref and edit in one call, spike w03), then a dialog offers a pull request into the viewed branch (`POST pullrequests`) and opens it. Share link (share_plus) on the file/folder action sheet and the commit menu. Verified on the scratch repository 2026-09-11: README.md edited in the app, committed as a8c9baa on `kkamm/readme-0911-1024`, PR **8336** created and opened in the app (left active for inspection). Repos plan phases 0–5 are complete; remaining ideas: commit list filters, jump-to-line from search results once the service exposes offsets, in-editor find, a share action on the repo page.
16. **iOS walkthrough (2026-09-11, Mac, iPhone 17 + iPad Pro 13" simulators, debug builds, Kelly signed in with the puremedia account; writes only in DevOps Mobile App).** Fixed on the way and committed: the vendored MSAL Swift did not compile (`MSALClaimsRequest(jsonString:error:)` takes an error pointer); go_router's debug check rejected the tab shell's parameterized branch defaults, so every debug build red-screened at launch (placeholder `initialLocation`s plus a router test); the favorites toast never dismissed and blocked every later snackbar (Flutter 3.47 persists action snackbars: `persist: false`, rule in DESIGN.md); the work item list row subtitle overflowed at accessibility text sizes (now a `Wrap`); unknown code search infoCodes get a plain message (the scratch project answered 15); the Diagnostics page shows an Environment card (breakpoint, text scale, accessibility flags). Verified on iOS: browser sign-in, Graph account header, org and project tiles, activity feed with the notification prompt and its deep link into a work item, four-tab shell (bottom bar on the phone, rail and two-pane Work/code browser on the iPad in landscape), work item detail/edit (WebView editor), comment, state change, title save, board with split columns and lanes, repos list with language line and favorites (synced across devices), repo page, code browser, file viewer (Markdown preview and source), commits, commit page, commit diff, branch picker with stats and tags, browse at tag, PR inbox (org-wide and repo-scoped), PR overview with Checks, Files with iteration picker, file diff with threads, thread reply / resolve / reactivate, vote and reset, conversation comment, pipelines runs/definitions/approvals, environment approval, run queue, live polling, cancel, run page menu, log page, dark mode, xxxL text. Test data touched: 15503 title now ends in " iOS" and has one more comment; 15506 went Resolved and back to Active; PR 8334 has a reply and a conversation comment and Kelly as a reviewer (no vote); run 20260911.6 was approved and succeeded, 20260911.7 was queued and canceled. **Open findings** (owner: Kelly to prioritize): ~~(a) refresh buttons still on the work item, PR detail, PR diff and log pages~~ **fixed 2026-09-12**: no `tooltip: 'Refresh'` is left in the app. The PR detail's three tabs, the log and the file diff pull to refresh instead; short pages needed `AlwaysScrollableScrollPhysics` before the gesture would fire at all, and the diff (which also scrolls sideways) takes the board's `notificationPredicate` so only a vertical pull on the rows counts; ~~(b) reviewer and approver identities show initials~~ **fixed 2026-09-12**: reviewers carry no `descriptor` (spike s23), so `IdentityRef` now takes it from the `_links.avatar` MemberAvatars link and the Graph avatar loads; confirmed by Kelly on the emulator; ~~(c) Home reloads only on first build~~ **fixed 2026-09-12** by `ReloadOnReturn` (`lib/features/shared/`): the shell parks an inactive branch behind a disabled `TickerMode`, so the mode coming back on is the signal the tab is visible again; it reloads then, but only when what is on screen is older than two minutes, so tab switching stays instant. Wired into Home and reusable for the other tabs; covered by `test/features/reload_on_return_test.dart`; ~~(d) the Conversation tab hides file threads~~ **fixed 2026-09-11**: it kept only threads with no `threadContext`, so a review held entirely on lines showed as empty (spike s22: CloudCover 2.0 / ServiceDelivery !8261 has 52 file threads, 14 active / 37 resolved / 1 pending, and no others). The tab now lists every human thread oldest first, each file thread headed by "file.js:38" that opens the diff there, with All / Active / Resolved filter chips carrying counts (Kelly's ask). Same day, resolving made reachable (Kelly: "how would a reply and resolve work cleanly"): an open thread shows Reply beside a direct Resolve; the composer shows Cancel + Resolve while the box is empty (so nothing typed is ever discarded) and Cancel + Reply + "Reply & resolve" once there is text, reading "Reply & reactivate" on a settled thread. The pair is two calls, so a reply that posts when the status change fails keeps the comment and says so in a snackbar, and a failed reply never changes the status; `test/features/thread_card_test.dart` covers the four states and spike w15 verified all four against scratch PR 8334 on 2026-09-12 (threads 42511, 42512, 42566, read back through the API); ~~(e) "Conversation (1)" tab label clips on the phone~~ **fixed** the same day by renaming the tab to "Comments"; ~~(f) the diff's line-number gutter scrolls away horizontally~~ **fixed 2026-09-12**: the gutter and the thread cards are pinned (moved back by the horizontal offset, painted over the code) so only the code pans; confirmed by Kelly; (g) the work item detail's label column breaks "Changed" mid-word at accessibility text sizes; (h) full-width fixed snackbar on the iPad (floating with a max width would fit); ~~(i) repo-scoped PR list defaults to To review~~ **fixed 2026-09-12**: a list scoped to one repository opens on All active, while the org-wide inbox stays a review queue and opens on To review; (j) the PR composer stays docked under the Files tab; (k) optional failing policies use the same red X as blocking ones; ~~(l) the sign-in tagline says "on your phone" on the iPad~~ **fixed 2026-09-12**: the tagline follows the window width; (m) board drag-and-drop was not exercised on iOS (idb has no long-press drag; check by hand). Still to run on a physical iPhone: Authenticator broker, Conditional Access, notification delivery. Kelly's tablet feedback the same day: the Material rail's solid column cut into the app bar, so on iOS and macOS the shell now uses `GlassNavigationRail` (`lib/features/shared/widgets/`): a floating, blurred, translucent pill over the unchanged page background (`Radii.xl`), vertically centered at 80% of the height with the four destinations spread evenly along it (Kelly's second round of feedback), layered above the body in a Stack with a 24 dp margin so its shadow rests unclipped (third round), with vertical pages padded clear of it and sideways scrollers (the board, `ProjectShell._bleedsUnderRail`) given the gutter as left MediaQuery padding so columns rest clear and slide under the glass when scrolled (blur plus saturation boost, thin tint, specular top highlight; fourth and fifth rounds); in portrait the rail lies along the bottom at 80% of the width and pages get its height as bottom MediaQuery padding, so lists scroll under it and clear it at the end, Kanban cards included (sixth round); in landscape the rail sits on the right by default with a Settings > Appearance switch to move it left, persisted by `ThemeController` (seventh round), verified on the iPad in light and dark; Android keeps the Material rail. Wider single-page views (Kelly, same day): `ContentColumn` follows the window up to 1120 dp instead of stopping at 840, and Home, the repo page and the PR overview lay their sections side by side (`SideBySide`, from 960 dp of content width). iPhone in landscape (Kelly, same day): the glass rail showed as a 13 pt sliver and text ran under the Dynamic Island, because the rail's `SafeArea` sat inside its own 72 pt box (shrinking it by the 59 pt side inset instead of moving it) and no page applied the side insets at all. The Apple chrome now lives in `GlassShellLayout` (pure layout, `test/features/glass_shell_layout_test.dart` checks it at phone and tablet insets): the page keeps clear of the sides with a `SafeArea`, the rail sits outside those insets, the board keeps the insets plus the gutter as padding and slides under both, the rail is held to at least 80% of the safe height and grows only for larger text instead of overflowing, and the four pages outside the shell (organizations, projects, settings, diagnostics) wrap their body in `SafeArea(top: false, bottom: false)`. The Android rail row got the same SafeArea, not yet checked on a device. Verified on the iPhone 17 simulator in landscape (light, dark, xxxL text) and on the iPad in both orientations, unchanged. Same session, island on the left: the rail on the right then sat the full inset plus its margin from the edge (Kelly: way too far). iOS reports the same inset on both sides, so `DisplayCutout` (`lib/core`) asks the Runner for the interface orientation over a method channel in `AppDelegate.swift` (`com.kammcs.boardhop/display`, read again on every metrics change) and `GlassShellLayout` keeps only the margin from the island-free edge, clearing the inset only on the island's side or when the side is unknown (Android, tests). `tool/shot-ios.sh` gained `ROT=right` for that orientation.
17. **TestFlight (2026-09-11, Kelly's ask; ships the way multipass does, see `../multipass-theater/docs/runbooks/RELEASE.md`).** Done on the Mac: Runner signs automatically on team **73W98CESN9** ("Kelly Kamm", Apple ID **syndalis@mac.com**), `ios/ExportOptions.plist` (app-store-connect, automatic, export, symbols), `ITSAppUsesNonExemptEncryption=false` in Info.plist (HTTPS only), and `tool/ship-ios.sh <build-number> [--skip-build]` which runs `flutter build ipa` with the `.env` defines, checks the exported entitlements, and uploads with `xcrun altool --upload-app` as syndalis@mac.com using the app-specific password (read at run time from the gitignored `.env.asc` as `BOARDHOP_ASC_PASSWORD`, else from the multipass repo's `.env`; never committed or printed). The first signed archive + App Store export succeeded (1.0.0 build 1, 41 MB): automatic provisioning registered `com.kammcs.boardhop` on the team and the store build carries `73W98CESN9.com.kammcs.boardhop` plus the `com.microsoft.adalcache` keychain group. Kelly created the App Store Connect record the same day: **BoardHop**, bundle com.kammcs.boardhop, Apple ID **6811138100**, SKU com.kammcs.boardhop. First upload 2026-09-11 13:59: 1.0.0 (1), delivery UUID 50b073db-3380-4479-87f3-bb6f6723b7a8, `tool/ship-ios.sh 1 --skip-build`. Build 1 came back with ITMS-90683: missing NSPhotoLibraryUsageDescription and NSCameraUsageDescription (required) and NSLocationWhenInUseUsageDescription (advisory), referenced by html_editor_enhanced's file picker and flutter_inappwebview even though the editor has picture/audio/video/file insertion off. Info.plist now carries purpose strings that say Boardhop does not use them and never prompts; build 2 uploaded (delivery UUID 00bcaa8e-230b-4da9-8bd7-e35831861fed). Build 3 (delivery UUID cd2947d3-d4a4-4a79-be8d-7850feaf922d, 2026-09-11, `tool/ship-ios.sh 3`) carries the landscape safe-area and rail fixes; it was VALID and in internal testing within minutes, auto-distributed to the group. Kelly made the internal group "BoardHop Testers" and joined it after build 2 had landed; App Store Connect left the tester at NOT_INVITED with no resend option and no mail. Kelly then added an **Admin App Store Connect API key** (`AuthKey_YD5P39439T.p8` at the repo root, gitignored via `*.p8`; issuer id = the team's public id); `POST betaTesterInvitations` through it sent the invitation (state INVITED). `tool/testflight.rb status | invite <email> | notify` wraps those calls for next time. Later, before external testers or review: App Privacy answers, a `PrivacyInfo.xcprivacy` for Runner, screenshots and the listing.
14. **Work item forms (planned with Kelly 2026-09-12; phases 0–3 landed, 4–5 to go).** Research and plan in `research/11-work-item-forms.md`: the app cannot create work items, and edits stop at title and description. Agreed design: a `+` left of the Items/Board pill (also a `+` per board column with state and lane pre-filled, and "Add child" from a work item), a type chooser with backlog types first and the rest under "Other" plus team templates, a form engine built from the Processes layout API (pages, sections, groups, controls, custom fields in web order) with a field-list fallback, controls per field type (picklist, identity picker from the type's allowed values, area and iteration tree pickers, tags, dates, rich text through the existing editor with a Markdown choice), state from the type's transitions, `validateOnly` dry runs with errors under their fields, then create. Phone: full-screen page with a pinned header and stacked collapsible groups; tablet: centered web-like dialog up to 960 dp with the sections in columns and page tabs. The same form replaces the edit page. Online only with local drafts. Links and attachments in the last phase. Spikes run the same day (s24, s25, s26 read-only; w16, w17, w18 in the scratch project; research/11 §9): the Processes layout API refuses every stock type (VS403115), but the type's `xmlForm` carries the full form for every type, so the engine parses that; a `validateOnly` create returns the server's defaults, so the form opens from a dry run; a new item can only start in its initial state, so a board-column create is create-then-patch; parent links, tags, Markdown and attachments all ride in the create call (#15539, #15540, #15541 created). The Assigned To allowed-values list is empty in both projects, so the people picker is now team members (cached, recent first) plus a project-scoped Graph subject query as you type (Kelly, same day). **Phase 0 landed 2026-09-12** (no UI yet): `lib/data/models/work_item_form.dart` (`FieldSpec`, `FormLayout` parsed from `xmlForm` through the new `xml` dependency, `FormSpec`, `ClassificationNode`, `TeamDefaults`, `WorkItemTemplate`, `BacklogTypes`, `ValidationError` / `WorkItemRuleException`, `WorkItemDraft`; `WorkItemType` gained `transitions` and the parsed layout and drops the raw XML) and `lib/data/repositories/work_item_form_repository.dart` (`formSpec` with the field-list fallback, classification nodes, team defaults, tags, templates, backlog types, team members and the Graph people search, `validate` / `create` / `validatePatch`, attachment upload, link-target search, drafts, and the pure patch builders), all cached 24 h in `cache_entries` under account-namespaced `form:*` keys. Fixtures for the tests come from the read-only spike `s27_form_fixtures.py` (scratch Bug and User Story) in `test/fixtures/forms/`; 57 new unit tests in `test/data/work_item_form_*_test.dart` and `work_item_draft_test.dart`. Spike `s28` confirmed `graph/storagekeys/{descriptor}` resolves a Graph descriptor to the identity id, but spike **s29** (2026-09-12, read-only) then found that the work item store **refuses that id** for `System.AssignedTo` ("unknown identity"), so `assignedToValue` now sends `"Display Name <unique>"` and keeps the id only for an identity without a unique name.

    **Phase 1 landed 2026-09-12** (type chooser, the `+` in the Work app bar, the phone create form). `lib/features/work_items/form/`: `new_work_item_button.dart` (a `+` immediately left of the Items/Board pill on the work items page and the board, icon only on compact and icon + "New" from medium up, which reloads the list or board when the form closes); `type_chooser.dart` (bottom sheet on a phone, anchored menu from medium up; backlog levels top-down — a level the team hid still offers its type, only `Microsoft.HiddenCategory` hides one — then a collapsed "Other", with the project's last used type pinned under "Recent" and a type's templates expanding to "Blank" plus the names when the team has any); `work_item_form_page.dart` (route `…/work-items/new?type=&team=&state=&lane=&laneField=&parent=&template=`, placed before `:id` in the router; loads the spec, the team defaults and a `validateOnly` dry run whose fields become the initial values under the pre-fills; a parent copies its area and iteration and rides along as a `Hierarchy-Reverse` relation; `AdoAuthException` → `AuthInteractionRequired`, other failures inline with Retry); `work_item_form_state.dart` (values, dirty set, per-field errors, the 800 ms debounced dry run on a field with `dependentFields`, and the patch through phase 0's `buildCreateOps`); `work_item_form_body.dart` (pinned header: type chip, Title, then State — read-only on create, in its state-category colour — Assigned to, Area, Iteration and Tags chips; below it one card per labeled group of the Details page in web order with every column flattened top-down, groups of links/attachments/deployments panels shown as a disabled "Available after creation" card, custom pages under their own heading); `controls/` (text/number, allowed-value sheet with a free-text row where the field is not a declared picklist, date, switch, people picker with "Me" and "Unassigned" pinned plus team members and a 300 ms-debounced Graph search, area/iteration tree picker with the team's iterations first and the current one marked, tag chips with suggestions); `form_prefs.dart` (last type and recent assignees per project in `shared_preferences`). 14 new widget tests in `test/features/work_item_form_test.dart` and `type_chooser_test.dart` (215 in total). Verified on the phone emulator against the scratch project: chooser order, the form opening on the server's defaults (Area, Iteration 1, Priority 2), the people and tree pickers, tags, an empty Title blocked locally, a free-text Priority of 9 refused by the server under its own field with the form still editable, **Task #15542 created** (assigned to Kelly, Iteration 1, tag `phase1`, Priority 3, read back through the API) and the snackbar's **Open** landing on #15545. Not verified: templates (no team has any), a board or parent pre-fill (phase 4 sends them), the tablet arrangement (phase 2), and the html field is still a plain multi-line box that is stored as one escaped `<div>` until phase 2 brings the rich editor. Next: phase 2 (tablet dialog, rich text, Markdown choice). **Phase 2 landed 2026-09-12**: from medium up the `+` shows the form as a centered dialog (up to 960 dp, 90% of the height) over the live Work list or board, with the header on two rows and the layout's own sections as columns from `FormSection.percentWidth` (50/50 on the stock types, 60/40 for three, a fourth spanning beneath), custom pages as tabs across the layout area, and the phone arrangement inside the dialog when it is narrower than 640 dp; the `…/work-items/new` route renders the same box centered on its own page so a deep link still works (the route sits inside the Work branch, so a real scrim over the list is only possible from the `+`, which uses `showDialog(useRootNavigator: false)` to stay inside the account's repositories). `html_editor_enhanced` moved into a shared `HtmlFieldEditor` (`lib/features/work_items/widgets/`) used by both `WorkItemEditPage` and the new `controls/rich_text_control.dart`: an html field is now a card with the rendered value and a pencil that opens the editor full screen on a phone and as a large dialog on a tablet (Cancel/Done in the app bar), and `System.Description` on a new item offers a Rich text / Markdown toggle (monospace box with a Preview toggle, the format op on create, the choice remembered per project in `FormPrefs`, no conversion between the two ever). Editor content now goes into the patch verbatim (`setRichValue`), while a long-text field typed as plain text is still escaped into one `<div>`. Picklists drop an anchored `MenuAnchor` from medium up and the people and tree pickers open as centered dialogs. Three phase 1 review fixes: the people search is now scoped with the project **id** (`graph/descriptors/{name}` answers HTTP 400, so the query had silently gone org-wide — spike s30), the iteration picker's Team section lists every iteration of the team (`teamIterations`, cached 24 h, current marked) and the same person no longer appears twice in the people list. 8 new tests (223 in total), `flutter analyze` clean. Verified on the tablet emulator (dialog over the list in dark and light, two columns, anchored Priority menu, people dialog with a project-scoped Graph hit, iteration picker with Iterations 1-3, the phone arrangement inside the dialog at a 650 dp window) and on the phone (full-screen HTML editor with bold, Markdown toggle and preview): **User Story #15546** (tablet, HTML description `<p>… <b>bold</b></p>`), **Bug #15547** (phone, HTML repro steps) and **Task #15548** (phone, `multilineFieldsFormat` **markdown**, `**bold** list`), all read back through spike s31 and rendering on the detail page. Not verified: the page tabs (no process in reach has a custom page, so they are covered by a widget test only) and the iPad simulator. Next: phase 3 (edit through the same form, transition-driven state sheet). **Phase 3 landed 2026-09-12**: the pencil on a work item now opens the **same full form in edit mode** — `WorkItemFormPage.edit` on the route `…/work-items/:id/edit` (a dialog over the detail page from medium up, full screen on a phone), loaded from the item itself with every control the layout has, read-only fields shown as text when they carry a value, the long text kept in its own format and never re-escaped, an editable **State chip over the type's legal transitions** that reveals **Reason** under it when the state moves, Save disabled until something changes, `validatePatch` then `patch` with only the changed fields and `test /rev`, rule errors under their fields, and an **HTTP 412 conflict banner** with Reload (keeps the typed values on top of the fresh copy) and Discard; success pops back to the detail page, which refreshes and says "Saved". The form stays online-only: with no connection the cached item opens read-only behind "Editing needs a connection" with a Retry, while the detail page's quick actions (state, assign, comment) keep the write queue. `WorkItemEditPage` is deleted (`HtmlFieldEditor` stays); the detail page's state sheet lists `transitionsFrom(state)` — free, because the cached type list already carries `transitions` (spike **s32**) — and asks for a Reason in a second step where a process marks it required (no stock type does). Two phase 2 review fixes: re-tapping the current tab no longer throws an open form away (the new `UnsavedWork` registry lets `ProjectShell` ask with the form's own discard dialog, and a re-tap at the branch root does nothing), and the `+` no longer stays disabled after the form closes. 12 new tests in `test/features/work_item_form_edit_test.dart` (**235** in total), `flutter analyze` clean. Verified on the phone emulator against scratch items: **#15542** edited (title + " (edited)", Priority 1, New → Active, the server chose Reason "Work started"), **#15543** given a real 412 by spike `w19_bump_rev.py` — banner, Reload kept the typed title, Save succeeded on the new revision — the tab re-tap prompt, and the offline banner with Retry under airplane mode; **#15544**'s state sheet on the detail page; and on the tablet emulator **#15546** edited in the dialog (Story Points 5). All read back through spike `s33`. Not verified: a process that marks Reason required, a read-only field on a card (no stock type has one — widget test only), and the iPad simulator. Next: phase 4 (board column `+`, "Add child", templates, drafts). **Phase 4 landed 2026-09-12** (board column `+`, Add child / Add related, templates, drafts). Every board column now ends in a quiet `+ New item` row (`lib/features/boards/widgets/new_card_row.dart` through the new `KanbanBoard.columnFooterBuilder`; a split column shows it in its Doing half only) which opens the form with the column's `stateMappings[type]`, the lane on screen (the board's `fields.rowField` plus the selected lane, skipped for All lanes and the default lane) and **the board's team** passed through the route as `team=`; the board's types are the only ones offered, and a board with a single type skips the chooser. Save is **create then patch** (spike w18: only the initial state is legal on create): the create carries everything but State, then `boardFollowUpOps` writes State (and the lane field, which the layout does not carry) in one `test /rev` patch; a failed second call keeps the item and says so — "#N created in New (could not move to Active)" with **Open** — and the board reloads either way, tinting the new card for 1.8 s (`WorkItemCard.flash`). The detail page gained an overflow with **Add child** and **Add related** (`…/work-items/new?parent=15546&rel=child|related`): the child's type is the backlog level below the parent's (`BacklogTypes.childTypeNames`, Bug follows `bugsBehavior` because the service already places it), the form pre-fills the parent link, Area and Iteration and shows "Child of #15546 - <title>" under the type chip, and the page now lists a compact **Links** row (parent and children with titles through one `batch`; `WorkItem` keeps the `relations[]` of the `$expand=all` read, which it used to drop). Spike **w20** created the first team template — **Boardhop spike task** (Task, id `edae8a24-9850-401b-9d0c-7df212025c56`, left in place on the DevOps Mobile App Team) — so the chooser's template expansion is exercised at last: the list read carries no `fields`, so "Blank" plus the names come from the list and the values from `templates/{id}`; a stale **empty** template list is now re-read once per session (the day-old empty copy hid the new template) and the title keeps a template's trailing space with the caret after it. **Drafts** (create only): closing a dirty form asks Keep draft / Discard / Cancel and a network failure offers Keep draft in its banner; the draft keeps the values the patch would send plus the formats, relations and pre-fills, the chooser lists `Resume draft: Task - Half typed - 5m ago` with a delete icon, resuming marks everything dirty (and long text rich, so it is never escaped twice), a successful create clears it and one older than 30 days is dropped on read. 26 new tests (`board_new_item_test.dart`, `child_type_test.dart`, `draft_test.dart`, plus the chooser and form suites), **261** in total, `flutter analyze` clean. Verified on the phone emulator against the scratch project: the Active column's `+` created **User Story #15549** (the Stories board offers User Story only, so no chooser; the card landed in Active/Doing at 2 / 5 after the follow-up patch), **Add child** on #15546 offered Task and Bug and created Task **#15550**, whose parent's Links row then reads "Children (1) #15550"; the Work `+` then showed **Blank + "Boardhop spike task"** under Task and the template filled the form (title `"[template] "` with the caret after the space, Priority 1, tags template/boardhop) into Task **#15551**; and a dirty form closed with **Keep draft** came back as "Resume draft: Task - [phase4] draft resumed - just now", resumed dirty, created Task **#15552** and left the chooser without a draft row. Everything read back through the read-only spike `s34_phase4_items.py`: #15549 rev 2 Active with `Kanban.Column` Active and `Column.Done` false (the service derived the column from the state, spike w02) and the process rule assigned it to Kelly, #15550 carries `Hierarchy-Reverse` to 15546 while 15546 carries `Hierarchy-Forward` to 15550, #15551 has Priority 1 and tags `boardhop; template`. On the tablet emulator: the `+ New item` row in every column but a split column's Done half, the form as a dialog over the live board from the New column's `+` (state chip New) and **Add child** from #15549's overflow, whose dialog reads "Child of #15549 - [phase4] from the Active column" (nothing created from the tablet). Not verified: an item created from a lane-filtered column (the scratch lanes hold the two older stories), a failed follow-up patch (the snackbar path is covered by a test only) and the iPad simulator. Next: phase 5 (Links and Attachments pages).
18. **In parallel, administrative:** start Partner Center enrollment; register the boardhop domain; Apple/Google developer accounts.
19. **Later:** Marketplace extension + tenant relay (research/06; this is also where live task log tailing lives, since only the web's SignalR feed carries running output, see research/06 "What it buys"), webhook payload capture spike, Intune plugin only if a customer requires it (research/08 §4).

## Conventions

- `FilledButton` minimum size is 48×48 in the theme; never `Size.fromHeight`, which is an infinite width and breaks buttons inside a `Row`. Full-width CTAs stretch through a `Column(crossAxisAlignment: stretch)` or a `ListView`.
- Design rules live in DESIGN.md: one master theme in `lib/theme/`, light and dark both first class, colors only through `Theme.of(context)` and `context.boardhopColors`, spacing and radii from tokens, every screen checked at compact and expanded width.

- Never commit client IDs, tokens or raw spike results. `.gitignore` already excludes `research/spikes/results/*.md` except the README, plus `.env` and `android/secret.properties`.
- After changing drift tables run `dart run build_runner build`; the generated `app_database.g.dart` is committed.
- Spikes against puremedia: writes only in the "DevOps Mobile App" project. Run with `python research/spikes/_run_with_mcp_creds.py <script>`.
- Commit messages end with `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`.
- Azure DevOps API: pin `api-version=7.1` per operation; read work items without a `fields` filter when the format map is needed; always send `test /rev`.
