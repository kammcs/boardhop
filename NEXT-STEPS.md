# Boardhop — state and next steps

**As of:** 2026-09-12, iPhone dock rebuilt to Apple's tab-bar geometry (step 16); 2026-09-11 first iOS build and full walkthrough on the iPhone 17 and iPad Pro simulators (step 16); milestones 1–3 and the repos plan done on Android.
**Read first:** [research/00-feasibility-summary.md](research/00-feasibility-summary.md) section 0 (decisions) and 6a (spike results), then [research/08-stack-comparison.md](research/08-stack-comparison.md).

## Where things stand

| Area | State |
|---|---|
| Product | Boardhop: Flutter mobile client for Azure DevOps Services, iOS + Android, phones + tablets. Jira-style boards and work items, GitHub-style PR review. |
| Stack | **Flutter**, locked 2026-09-10. `msal_auth` for Entra sign-in with the Authenticator broker; `drift` for offline cache and write queue; `html_editor_enhanced` for rich text (chosen by spike F3); `flutter_widget_from_html` and `flutter_markdown_plus` for rendering; `diff_match_patch` + `re_highlight` + custom diff viewer. |
| Scope | Entra OAuth only at launch (no PAT). Cloud only. Read plus lightweight writes in v1. Responsive tablet layout, multi-pane later. Free app; revenue via a Marketplace extension plus a relay in the customer's Azure tenancy. Build order: work items and boards, then PR review, then pipelines. |
| Research | Documents 01–09 in `research/`, all committed. Spike scripts and consolidated results in `research/spikes/` (raw result files are gitignored: client data). |
| Spikes done | Org-level PR list exists; `multilineFieldsFormat` returned at 7.1 without a `fields` filter; WEF column write derives State; `validateOnly` gives field-level errors; stale rev → 412; line comments track across pushes when read with `$iteration`/`$baseIteration`; ```` ```suggestion ```` fence renders as an applyable suggestion; poll cycle ≈ 0.013 TSTU; profile/accounts APIs on `app.vssps` are Entra-only. |
| Entra | kammcs tenant exists (from the Multipass code-signing project). **Boardhop registration created** as multi-tenant public client with Azure DevOps delegated permissions (`vso.threads_full` is not offered in Entra; `vso.code_write` covers PR threads). **Puremedia admin consent granted** for tenant `342d4cd1-7ea8-4452-8ceb-542b71d159f6` (verified domain `cloudcover.it`); the trailing browser error was the redirect to the `msauth` URI, not a failure. |
| Test org | `https://dev.azure.com/puremedia`, project "CloudCover 2.0" for read tests; scratch project **"DevOps Mobile App"** for every write (work items #15503–#15507 and #15539–#15541 from the form spikes; PR 8319 completed 2026-09-11, scratch PR **8334** seeded by `w09` with a line comment and a suggestion; pipeline **boardhop-scratch** (id 139) on the hosted `Azure Pipelines` queue with environment `boardhop-scratch` (id 18) behind an approval check, Kelly as approver). PAT in the AzureDevOps MCP config is authorized for spikes; it reads the Build API but cannot create agent queues. |
| Repo | `git@github.com:kammcs/boardhop.git`, branch `main`. Commit and push are pre-approved. |
| Emulator | AVD `boardhop_pixel_10_pro` (Android 17, Play image). Start with `tool/start-emulator.ps1`; the `-dns-server` flag is required on this host. |
| iOS | Built and signed in on the Mac 2026-09-11 (Xcode 26.6, iOS 26.5 simulators, SwiftPM, no Podfile). `tool/shot-ios.sh` drives the simulators through idb. Still untested: physical device, Authenticator broker, Conditional Access. |
| Plugin fork | `packages/msal_auth` is a vendored, patched copy of msal_auth 3.5.3 (path dependency). Keep the diff small; see its README. |
| Kanban board | Spike F4 done 2026-09-10: no package. `lib/features/diagnostics/board_probe/hand_rolled_board.dart` (Flutter `LongPressDraggable` + `DragTarget`, auto-scroll both axes, insertion gap, haptics, drag session owned by a `Listener`) is the seed for the real board; measured 2.1% janky drag frames at 200 and 1,000 cards in a profile build on the emulator. |
| Diff viewer | Spike F5 done 2026-09-10: `lib/features/diagnostics/diff_probe/` holds the seed (Myers `LineDiff`, `CodeHighlighter` over `re_highlight`, `PrDiffSource` for iterations/changes/items/threads, row widgets on `super_sliver_list`). |
| Pull requests | Milestone 2 done 2026-09-10 except Complete/Abandon (untested): inbox with offline cache, detail with Checks, iteration picker, file diffs with tracked threads, replies and thread status, suggestion apply (commits through the Pushes API), vote. |
| Pipelines and activity | Milestone 3 first pass 2026-09-10: `lib/features/pipelines/` (runs, definitions with Run, approvals, run timeline with polling, logs; writes untested until the scratch project has a pipeline), `lib/features/activity/` (polled feed with a last-seen marker) and local notifications (`lib/core/notifications/`, `lib/data/activity_sync.dart`). |
| Design system | Done 2026-09-10: `BoardhopTheme` light/dark from one seed, `BoardhopColors` extension for work item / PR / pipeline / diff colors, tokens, breakpoints, `ThemeController` (system default, persisted; Settings > Appearance). Rules in DESIGN.md. |
| App scaffold | Done. Flutter 3.47.3 / Dart 3.13. `lib/core` (config, `AdoClient`, rate limits, typed errors), `lib/auth` (`AuthService` over `msal_auth` 3.5.3, `AuthBloc`), `lib/data` (drift: organizations, projects, pending_writes), `lib/features/*` (sign-in, org picker, project list, diagnostics; placeholders for work items, boards, PRs, pipelines, activity). Android: `assets/msal_config.json`, `BrowserTabActivity` with the hash from gitignored `android/secret.properties`. iOS: URL scheme, `LSApplicationQueriesSchemes`, keychain group entitlement, deployment target 16.0. 13 unit tests pass; `flutter analyze` is clean. |
| Still administrative | Partner Center + MPN ID for publisher verification; Visual Studio Marketplace publisher; Apple and Google developer accounts; privacy policy; domain for boardhop. |

## Values needed from Kelly

- ~~Entra client ID~~ Provided in the gitignored `.env` as `BOARDHOP_CLIENT_ID`; run with `--dart-define-from-file=.env`.
- Confirmation that `msauth.com.kammcs.boardhop://auth` and the Android debug redirect `msauth://com.kammcs.boardhop/%2F%2Fksb0DQrePXmmxPydZ%2FUbpze98%3D` are saved on the registration.
- A physical device with Microsoft Authenticator for the broker path and Conditional Access (the emulator run used the browser fallback).

## Next steps, in order

1. ~~**Scaffold the Flutter app**~~ Done (see the App scaffold row above). CI: not set up. A GitHub Actions workflow was added and removed the same evening (2026-09-10) because Actions minutes are a budget item; Kelly decides if and where CI runs. `flutter analyze --fatal-infos` and `flutter test` pass on a clean clone (analysis excludes `packages/**` and `research/**`, which carry their own pubspecs).
2. ~~**Spike F1**~~ Passed 2026-09-10 on a Pixel 10 Pro / Android 17 emulator (`tool/start-emulator.ps1`, then `flutter run -d emulator-5554 --dart-define-from-file=.env`): browser-fallback sign-in, silent restore after force-stop, org discovery, 2 KB token with ten `vso.*` scopes, all diagnostics green. See `research/spikes/results/README.md`. Still open: the Authenticator broker path and iOS, which need a physical device.
3. ~~**Spike F2**~~ Done 2026-09-10. `msal_auth` is vendored at `packages/msal_auth` (3.5.3+boardhop.1) with `claims`, `forceRefresh` and `clientCapabilities` added; CP1 is declared; `AdoClient` retries a 401 once via `AuthService.resolveChallenge`. Emulator run: forceRefresh and claims requests both reach native MSAL. Token lifetime stays ~75 min with CP1 (no long-lived CAE tokens from Azure DevOps). Left open: trigger a real revocation to see an `insufficient_claims` 401 end to end; compile the Swift side on a Mac; offer the patch upstream.
4. ~~**Spike F3**~~ Done 2026-09-10: `html_editor_enhanced` (WebView) is the editor; Delta-based editors destroy tables and mentions on real content. See research/spikes/results/README.md. Production notes: set content via `evaluateJavascript` with a JSON-encoded string, keep the editor's slot in its parent stable, load attachment images with an auth header, and watch the `flutter_inappwebview` beta override until 6.2 ships stable.
5. ~~**Spike F4**~~ Done 2026-09-10: no package. `drag_and_drop_lists`, `appflowy_board` and `boardview` all jank far more than Flutter's own `LongPressDraggable` + `DragTarget` on the same emulator battery (2.1% janky drag frames vs 15–25%), and each carries dependency risk; `super_drag_and_drop` would need a Rust toolchain everywhere. The hand-rolled board in `lib/features/diagnostics/board_probe/` (edge auto-scroll on both axes, insertion gap, haptics, 1,000 cards fine) is the seed for the real board. See research/spikes/results/README.md for the production notes, above all: own the drag session in a `Listener`, since a `Draggable` unmounted by auto-scroll never reports drag end.
6. ~~**Spike F5**~~ Done 2026-09-10: own Myers line diff (`LineDiff`; `diff_match_patch` only for intra-line emphasis), `re_highlight` whole-file highlighting split per line, `super_sliver_list` rows (exact jump-to-line in 101 ms; `ListView.builder` needs 902 ms and misses by 50 lines), tap-to-comment gutter, threads overlaid from `$iteration`/`$baseIteration` (verified on PR 8319: both w03 threads land on their tracked lines). See research/spikes/results/README.md. Production notes: highlight off the UI thread (376 ms for two 3,000-line files), collapse unchanged regions, cache content by SHA, composer write path with `changeTrackingId`.
7. **First milestone (in progress, 2026-09-10).** Done: project shell with bottom navigation / rail (Work items · Board · Pipelines), "assigned to me" list (WIQL `@Me` → `workitemsbatch`, cached in drift under a list key), work item detail (full read without a `fields` filter so `multilineFieldsFormat` is honoured; HTML through `flutter_widget_from_html_core` with bearer-authenticated attachment images, Markdown through `flutter_markdown_plus`; discussion from the preview Comments API rendered from `renderedText`), Kanban board (board definition + team area paths + WIQL ordered by StackRank/BacklogPriority with fallback, cards distributed by the WEF column field, drag-and-drop on the shared `KanbanBoard` widget, optimistic column move written as WEF column + mapped state with `test /rev`, reverted with the message on failure). Types and state colors come from `workitemtypes` and are tinted for dark mode. Also done: comment posting (`format=markdown`), state picker from the type's states, assign to me / unassign, all rev-guarded with rule errors shown per field (verified on scratch task 15503). Board, second pass: split columns render as Doing/Done slots (WIP count over the whole column), swimlanes as a filter chip row with a lane badge on cards, and every drop writes the rank through `workitemsorder` (the moved card plus its run of unranked neighbours, bounded by ranked cards; verified on the scratch board, ranks assigned to both stories). Split columns and lanes were verified on the emulator 2026-09-11 after Kelly became team and project administrator of the scratch project and `research/spikes/w05_board_config.py` configured its board (Active split into Doing/Done with WIP 5, an "Expedite" swimlane holding 15507): the lane chips, the Doing/Done halves and the 1 / 5 count render as designed. Work items tab has a list chooser: assigned to me, recently updated (7 days), or any saved query from the first two levels of My/Shared Queries (flat, tree and one-hop results handled), each cached under its own list key. Title and description editing through the F3 editor (HTML) or a Markdown field, only changed fields written. Offline queue: a network failure on a move, field edit, title/description edit or comment queues the write in the `pending_writes` table, applies it to the local cache, and a banner in the project shell shows what waits; the queue drains on open, on app resume and on Retry, replaying each write with its original `test /rev` so a 412 becomes a reviewable conflict instead of an overwrite (verified on the emulator with the radio off). Tablet: at medium and expanded widths the Work items tab shows the list beside the selected item (rail instead of bottom bar). Milestone 1 scope is complete.
8. **Milestone 2: pull requests (first pass done 2026-09-10).** Inbox (org-wide via the S4 endpoint, or per project as a shell tab) with To review / Created by me / All active; identity from `connectionData` (needs `api-version=7.1-preview`). Detail: Overview (Markdown description, reviewers with votes, linked work items), Files of the latest iteration, Conversation with a composer; vote menu, Complete and Abandon dialogs. File diff on the F5 engine (now in `lib/features/pull_requests/diff`) with threads read at `$iteration`/`$baseIteration` and a gutter composer that posts an anchored thread with `changeTrackingId`. Verified on scratch PR 8319: vote to Approved and back, line comment, conversation comment; CloudCover PRs read-only. Second pass (2026-09-10): a shared `ThreadCard` under diff lines and on the Conversation tab with inline replies (`POST threads/{id}/comments`, parented on comment 1) and a status menu (Resolve / Won't fix / Close / Reactivate via `PATCH threads/{id}`); Checks section on the overview from `mergeStatus`, `policy/evaluations` (7.1-preview.1, artifact id per spike s15) and the newest iteration's `statuses`, folded into one `PrCheck` vocabulary and tolerant of either read failing; iteration picker on the Files tab and in the diff app bar (changes and threads re-read for the chosen iteration, threads land on their iteration-1 lines); PR lists cached as JSON blobs in the `CacheEntries` table (schema v3) so the inbox opens offline and keeps the cached list with its age when the network read fails. Verified on the emulator: reply and resolve on the diff page, reply / won't fix / reactivate on the Conversation tab (PR 8319), Checks on a CloudCover PR (read-only), cached inbox with the radios off. Suggestion apply (2026-09-10 evening): a ```suggestion fence in a file thread shows "Apply suggestion" on the latest iteration of an active PR; it replaces the anchored lines (`rightFileStart..rightFileEnd`, line-ending style kept), commits through `POST repositories/{id}/pushes` with `refUpdates[].oldObjectId = lastMergeSourceCommit`, resolves the thread and reloads at the new iteration. Verified on PR 8319 (iteration 3 created from the app). Complete verified 2026-09-11 on PR 8319 (merged, source branch deleted; the page keeps reloading until the queued merge finishes). Abandon verified the same day on scratch PR 8334, which was then reactivated through the API for future tests. Not yet exercised: required-reviewer names in the Checks section. Then milestone 3: pipelines and the activity feed. Dogfood against puremedia (writes only in "DevOps Mobile App").
9. **Milestone 3: pipelines (first pass 2026-09-10).** `PipelineRepository` over the Build API (definitions with latest builds, recent runs newest first, run, timeline, log lines as a JSON string array), the Pipelines API for queueing (`POST pipelines/{id}/runs` with `resources.repositories.self.refName`), cancel (`status: cancelling`), retry stage (`PATCH builds/{id}/stages/{identifier}` with `state: retry`), and YAML approvals (`pipelines/approvals?state=pending&$expand=steps`, `PATCH` with `[{approvalId, status, comment}]`). Definitions and runs are cached like PR lists. Pipelines tab: Runs with a pipeline filter, Pipelines with a Run action (branch prompt), Approvals with approve/reject. Run page: header, stages → jobs → tasks (Phase wrapper flattened, siblings by `order`, classic builds with tasks under the root job), issues inline, polls every 8 s while active, Cancel / Run again / Retry stage. Log page with `##[error]`/`##[warning]`/`##[section]` accents, timestamp and wrap toggles. Verified read-only on CloudCover 2.0 (108 definitions, run list, a failed run's timeline and log); the Entra token covers `vso.build`. Writes verified 2026-09-11 on the scratch pipeline (created by `research/spikes/w06_scratch_pipeline.py` + `w08`): queue from the Pipelines tab, cancel mid-run (issues shown inline), retry the canceled stage (attempt 2), environment approval from the Approvals tab, Deploy stage ran after it. Lessons: a run rejected before an agent starts (pool not authorized) completes as failed with `validationResults` and no timeline, now shown on the run page; a pipeline's first use of an environment waits on `Checkpoint.Authorization` and the approval object does not exist until the pipeline is permitted for the environment (the run page says "waiting for resource authorization"); the approvals service returns ids as strings. Activity feed done the same day: `ActivityRepository` merges PRs to review, my PRs, one org-wide WIQL (assigned to me, changed in 14 days) plus a batch, and the last two days of builds from up to five "pinned" projects (those whose Pipelines tab was opened, found through the `pipelines:runs:` cache keys); cached per org with a last-seen marker; `ActivityPage` (bell icon on the project list) refreshes on open, pull, resume and every 60 s, filters by kind, shows a dot on new items. Verified on the emulator against puremedia. Local notifications done the same evening: `NotificationService` (flutter_local_notifications 22, opt-in in Settings > Notifications or the feed banner, Android 13 prompt, taps and cold-start launches routed in the app) and `ActivitySync` (polls the last-opened org every 3 minutes in the foreground and on resume, announces items newer than the last feed visit that were not announced before and not the user's own change, first run only sets the baseline, paused while the feed is open). Verified on the emulator: permission prompt, a "Build succeeded" notification after resume, tap opened the run. Android needed `POST_NOTIFICATIONS` and core library desugaring. Log tailing dropped 2026-09-11 after spikes s20/s21/w12/w13: the agent uploads a task's log only when the task finishes, even for 18 KB of output (the timeline record's `log` is null until then), so REST cannot follow live output and only the web's undocumented SignalR feed could; the run page's timeline polling already makes a task's log tappable as soon as it completes. Refresh buttons were removed from the pipelines, run, pull request and activity pages, which all pull to refresh. Classic release approvals deferred: spike s19 found five old classic definitions in CloudCover 2.0 and zero pending approvals anywhere in puremedia. Still to do: signed-URL log download for huge logs, iOS notification settings in `Info.plist` review. Tablet-width pass over the repos screens done 2026-09-11 (Home, Work, commits, commit page, branch picker, code browser two-pane): no layout defects.
10. **Style pass (2026-09-11).** People: `IdentityRef` keeps the Graph descriptor and `AvatarStore` reads `vssps …/graph/Subjects/{descriptor}/avatars` (base64 PNG; the `MemberAvatars` and `identityImage` links on identities refuse the Entra token), cached in memory and on disk under app support for 7 days, shown by `IdentityAvatar` over the initials. Organization page: `AccountHeaderBar` with the photo, email and company from Microsoft Graph (`User.Read` acquired silently, falls back to the Azure DevOps profile), cached. Organization and project tiles match the web (spike s16): projects use the service's own algorithm (.NET string hash into a 12-color palette, first-and-last-word initials with digits and punctuation dropped) drawn by `AdoTiles`, and show the project picture when the default team's Graph avatar is not `isAutoGenerated` (descriptor cached in the projects table, schema v5); organizations use the azure-devops-ui Coin scheme because the service has no image for them (the web's org rail may pick a different color). A project with a picture was verified 2026-09-11 after `w10` uploaded one to the scratch project: the tile shows the picture on the next app start (avatar misses are remembered for the session only).
11. **Multiple accounts (2026-09-11, Kelly's ask: kammcs and puremedia under different sign-ins).** MSAL runs in multiple-account mode (`account_mode: MULTIPLE`, `MultipleAccountPca`); `AuthService` keys tokens by account and tenant, `AuthBloc` holds the account list and signs accounts in (account picker) and out one at a time. Decisions with Kelly: an organization can appear under every account that reaches it (a person may hold an admin and a regular identity in the same org), so every org route is account-scoped (`/a/{account}/orgs/{org}/…`, `Routes`), the `/a/:account` shell route binds the repositories to that account (`AccountDeps`: client bound to the account, `JsonCache` namespaced for PR lists, pipelines, the activity feed and the account header, work item list keys prefixed, the write queue tagged with the account), background sync watches one org per account, and signing an account out wipes its organizations, caches and queued writes while keeping what another account also reaches. Organizations table is keyed by (account, name) (schema v6, recreated). The organization page shows a header row per account (photo, email, company, sign-out on the right) above that account's orgs, with "Add account" in the app bar. Verified on the emulator with one account; the second sign-in (kammcs) is Kelly's to type.
12. **Branding (2026-09-11).** Kelly's `assets/brand/logo.png` and `splash.png` (1254 px masters, logo baked into the splash's center). iOS: `LaunchScreen.storyboard` shows the splash as a full-screen `scaleAspectFill` image (`LaunchBackground` imageset), so the logo stays centered on any device or orientation. Android 12+: the system splash can only show a color plus an icon, so `values-v31/styles.xml` uses the artwork's edge color and an adaptive icon cut from the splash (`drawable-v31/splash_icon.xml`); older Android centers the artwork on that color (`launch_background.xml`, bitmap drawables cannot cover-scale). Flutter's `/` route is `SplashPage`, the same image with `BoxFit.cover`, so every platform lands on the identical look before sign-in state is known. Launcher icons come from `dart run flutter_launcher_icons` (config in pubspec: logo as the flat icon, splash artwork as the adaptive foreground). A larger master (2500 px or more) would keep the logo crisp on phones, where cover scaling enlarges it about 2.3x.
13. **Repos and code browsing (plan agreed with Kelly 2026-09-11; in progress).** Research and plan in `research/10-repos-and-code-browsing.md`: four-tab shell (Home / Work with Items·Board selector / Repos / Pipelines), repository list with language metrics and Azure DevOps favorites, repository page (PRs, branch row, Code, Commits, README), code browser and highlighted file viewer, branch picker with stats, commits and per-file diffs on the existing engine, code search. Decisions: Home = project overview, repo rows show language · branch · size, PRs open the existing list filtered by repo, Azure DevOps favorites, disabled repos badged at the bottom, branch remembered per repo, plus a tags page, share links and file editing (re_editor, <200 KB, commit to a new branch then offer a PR). Spike s17 verified every endpoint read-only; w11 (favorite write) runs on the scratch repo before phase 1. Order: shell first. Phase 0 (four-tab shell) and phase 1 (repository list, repository page with branch picker, README and PR link, favorites) landed 2026-09-11 and were verified on the emulator against CloudCover 2.0 and the scratch project. **Kelly:** add `vso.analytics` and `vso.profile_write` to the Boardhop registration and re-grant admin consent in puremedia (research/09 §A4) so the language line and the star work. Phase 2 (2026-09-11): code browser (`CodeBrowserPage`, one `OneLevel` listing per folder, folders first, breadcrumb, branch switch, long-press sheet with History / Copy path / Copy link / Open in browser) and file viewer (`FilePage`: metadata read, `blobs/{sha}` size gate at 1 MB with "Load anyway", images through `$format=octetStream` with the bearer token, binaries as a size-and-type card, text highlighted on a background isolate through `CodeHighlighter.highlightLinesAsync` with plain rows shown first, line numbers, wrap and text size, Markdown files rendered with a source toggle, the last 20 files cached by object id for offline reopening). README relative images and links now resolve through `RepoMarkdown`. Verified on the emulator against MonitorIT (CloudCover 2.0): folders, breadcrumb, Markdown preview and source, wrap, TypeScript and SVG-as-XML highlighting, a PNG, the action sheet. Not yet exercised: the large-file gate, a non-image binary, offline reopen, a cold deep link (`_RepoRoute` falls back to the network). Favorites toast (Kelly's ask, same day): starring or unstarring shows "{repo} added/removed from favorites" with a Show action that scrolls to the row's new section (the list moved onto `SuperListView` so an unbuilt row can be scrolled to by index) and tints it briefly; verified on the scratch repo. Phase 3 (2026-09-11 evening): commits list (`CommitsPage`, branch or path history, 50 per page with more on scroll, first page cached), commit page (`CommitPage`: message, author, parent chips, linked work item chips from `commitsbatch`, changed files with A/M/D/R badges, menu with browse-at-commit, copy id, copy link, open in browser), file diff between any two refs on the PR diff engine (`FileDiffPage`, highlighting on the isolate), branch compare (`ComparePage`: ahead/behind, common ancestor, changed files each opening a diff, reached from the picker's compare icon and the commits page), and a Tags tab in the branch picker (peeled to their commit, open the commit or browse files at the tag). `GitVersion` maps a ref string to branch, tag or commit version descriptors so the code browser and file viewer work at any of them. Verified on MonitorIT: list, commit page, edit diff against the parent, compare hyperv branch vs main (17 ahead / 4 behind, 30 files), tags list. Lesson: `commitsbatch` returns no `parents`; the single commit read does, so the page merges both. Phase 4 (2026-09-11, late evening): Home tab content (`ProjectHomePage`: pinned repositories from the Azure DevOps favorites, or recents when none; pull requests to review and of mine in the project; work items assigned to me; latest five runs, each section cached-first and failing on its own); code search (`CodeSearchPage` over `almsearch …/codesearchresults`, project-wide from the Repos app bar and the Home tab, repository-scoped from the repo page, results grouped by repository with match counts, paging on scroll, `infoCode` mapped to plain messages, 404 explained as the extension missing); a hit opens the file viewer with `find=`, which tints and scrolls to the first line containing the term since the service returns no line numbers; code browser shows the selected file in a right pane at medium and expanded widths (`FilePage(embedded: true)`). Verified on the emulator: Home sections against CloudCover 2.0, search for TODO (408 hits), a hit landing on its line, and the two-pane browser at 2000x1280. Phase 5 (2026-09-11, night): `FileEditPage` on `re_editor` 0.10 (same grammars as the diff highlighter, line numbers, dirty tracking, discard confirmation) edits text files under 200 KB from the file viewer's pencil; the commit sheet proposes `alias/stem-MMdd-HHmm` (sanitized for Git), the ref is created at the tip the app read and the change pushed to it (a push cannot create the ref and edit in one call, spike w03), then a dialog offers a pull request into the viewed branch (`POST pullrequests`) and opens it. Share link (share_plus) on the file/folder action sheet and the commit menu. Verified on the scratch repository 2026-09-11: README.md edited in the app, committed as a8c9baa on `kkamm/readme-0911-1024`, PR **8336** created and opened in the app (left active for inspection). Repos plan phases 0–5 are complete; remaining ideas: commit list filters, jump-to-line from search results once the service exposes offsets, in-editor find, a share action on the repo page.
16. **iOS walkthrough (2026-09-11, Mac, iPhone 17 + iPad Pro 13" simulators, debug builds, Kelly signed in with the puremedia account; writes only in DevOps Mobile App).** Fixed on the way and committed: the vendored MSAL Swift did not compile (`MSALClaimsRequest(jsonString:error:)` takes an error pointer); go_router's debug check rejected the tab shell's parameterized branch defaults, so every debug build red-screened at launch (placeholder `initialLocation`s plus a router test); the favorites toast never dismissed and blocked every later snackbar (Flutter 3.47 persists action snackbars: `persist: false`, rule in DESIGN.md); the work item list row subtitle overflowed at accessibility text sizes (now a `Wrap`); unknown code search infoCodes get a plain message (the scratch project answered 15); the Diagnostics page shows an Environment card (breakpoint, text scale, accessibility flags). Verified on iOS: browser sign-in, Graph account header, org and project tiles, activity feed with the notification prompt and its deep link into a work item, four-tab shell (bottom bar on the phone, rail and two-pane Work/code browser on the iPad in landscape), work item detail/edit (WebView editor), comment, state change, title save, board with split columns and lanes, repos list with language line and favorites (synced across devices), repo page, code browser, file viewer (Markdown preview and source), commits, commit page, commit diff, branch picker with stats and tags, browse at tag, PR inbox (org-wide and repo-scoped), PR overview with Checks, Files with iteration picker, file diff with threads, thread reply / resolve / reactivate, vote and reset, conversation comment, pipelines runs/definitions/approvals, environment approval, run queue, live polling, cancel, run page menu, log page, dark mode, xxxL text. Test data touched: 15503 title now ends in " iOS" and has one more comment; 15506 went Resolved and back to Active; PR 8334 has a reply and a conversation comment and Kelly as a reviewer (no vote); run 20260911.6 was approved and succeeded, 20260911.7 was queued and canceled. **Open findings** (owner: Kelly to prioritize): ~~(a) refresh buttons still on the work item, PR detail, PR diff and log pages~~ **fixed 2026-09-12**: no `tooltip: 'Refresh'` is left in the app. The PR detail's three tabs, the log and the file diff pull to refresh instead; short pages needed `AlwaysScrollableScrollPhysics` before the gesture would fire at all, and the diff (which also scrolls sideways) takes the board's `notificationPredicate` so only a vertical pull on the rows counts; ~~(b) reviewer and approver identities show initials~~ **fixed 2026-09-12**: reviewers carry no `descriptor` (spike s23), so `IdentityRef` now takes it from the `_links.avatar` MemberAvatars link and the Graph avatar loads; confirmed by Kelly on the emulator; ~~(c) Home reloads only on first build~~ **fixed 2026-09-12** by `ReloadOnReturn` (`lib/features/shared/`): the shell parks an inactive branch behind a disabled `TickerMode`, so the mode coming back on is the signal the tab is visible again; it reloads then, but only when what is on screen is older than two minutes, so tab switching stays instant. Wired into Home and reusable for the other tabs; covered by `test/features/reload_on_return_test.dart`; ~~(d) the Conversation tab hides file threads~~ **fixed 2026-09-11**: it kept only threads with no `threadContext`, so a review held entirely on lines showed as empty (spike s22: CloudCover 2.0 / ServiceDelivery !8261 has 52 file threads, 14 active / 37 resolved / 1 pending, and no others). The tab now lists every human thread oldest first, each file thread headed by "file.js:38" that opens the diff there, with All / Active / Resolved filter chips carrying counts (Kelly's ask). Same day, resolving made reachable (Kelly: "how would a reply and resolve work cleanly"): an open thread shows Reply beside a direct Resolve; the composer shows Cancel + Resolve while the box is empty (so nothing typed is ever discarded) and Cancel + Reply + "Reply & resolve" once there is text, reading "Reply & reactivate" on a settled thread. The pair is two calls, so a reply that posts when the status change fails keeps the comment and says so in a snackbar, and a failed reply never changes the status; `test/features/thread_card_test.dart` covers the four states and spike w15 verified all four against scratch PR 8334 on 2026-09-12 (threads 42511, 42512, 42566, read back through the API); ~~(e) "Conversation (1)" tab label clips on the phone~~ **fixed** the same day by renaming the tab to "Comments"; ~~(f) the diff's line-number gutter scrolls away horizontally~~ **fixed 2026-09-12**: the gutter and the thread cards are pinned (moved back by the horizontal offset, painted over the code) so only the code pans; confirmed by Kelly; (g) the work item detail's label column breaks "Changed" mid-word at accessibility text sizes; (h) full-width fixed snackbar on the iPad (floating with a max width would fit); ~~(i) repo-scoped PR list defaults to To review~~ **fixed 2026-09-12**: a list scoped to one repository opens on All active, while the org-wide inbox stays a review queue and opens on To review; (j) the PR composer stays docked under the Files tab; (k) optional failing policies use the same red X as blocking ones; ~~(l) the sign-in tagline says "on your phone" on the iPad~~ **fixed 2026-09-12**: the tagline follows the window width; (m) board drag-and-drop was not exercised on iOS (idb has no long-press drag; check by hand). **Second iOS walkthrough (2026-09-12, after pulling the Windows session's 18 commits; plan in `research/13-ios-walkthrough-plan.md`, reports in `research/walkthroughs/2026-09-12-iphone.md` and `-ipad.md`, one Opus agent per simulator, writes only in DevOps Mobile App: items 15554–15561 created, one reply each on PR 8334).** No exception, assert or overflow in either console. Verified on iOS for the first time: phase 2's tablet create dialog (two columns, anchored menus, people and iteration dialogs, rich-editor dialog with the Markdown toggle), phase 3's edit form and transitions, phase 4's board `+`, Add child, templates and drafts, phase 5's Links and Attachments (camera refused gracefully, library upload, full-screen viewer, Insert image), the Comments tab with filter chips, reply / resolve / reactivate, the repo-scoped All active default, pull-to-refresh on every page, dark, xxxL, portrait and both landscape orientations. **Fixed the same day** (two Opus fix agents, reviewed and committed here): (b) again — `PrReviewer.fromJson` read `descriptor` raw, reviewers carry none, so the avatar fell back to the 401 MemberAvatars URL (now from `IdentityRef.fromJson`; the reviewer row shows the photo); the people picker listed the current user twice (pinned Me plus the team list); the phone form stacked the Details "Related Work" panel and the Links section as two identical editable lists (the phone shows one; tablet tabs keep both); the detail page's Development and Deployment groups now say "Managed in Azure DevOps" like the form; Add child / Add related opened the phone sheet on a tablet (anchored menu now) and Add child was offered on a Task (hidden at the lowest backlog level); after Create on a tablet the two-pane list did not select the new item; library and camera picks kept `image_picker_<uuid>.jpg` (now `photo-<yyyyMMdd-HHmmss>.<ext>`); the attachment viewer sat under the shell (root navigator, dark, full screen); Tags, the State transition list and Add attachment were bottom sheets inside the tablet dialog (dialogs from medium up, ordered by the type's state order); a reply posted from the file diff was not reflected on the Comments tab (threads re-read when the diff pops); the glass rail swallowed the unsaved-work prompt when the tablet's form dialog was open at the branch root; at xxxL the rail follows the text scale up to 2× and the PR tab strip scrolls; draft row two lines; chooser menu constrained in phone landscape; filter-chip row follows the text scale; a resolved thread's plain button reads "Reply (reopens)". API fact recorded in research/spikes/results/README.md: **Azure DevOps reactivates a resolved thread when a comment lands on it**. **Walkthrough findings closed 2026-09-13** (Kelly's ask; iPhone 17 and iPad Pro 13" simulators, Xcode 27.0): **(g)** the detail label column was a fixed 104 pt, and a single word that does not fit its box is wrapped between characters, so "Changed" read "Chang / ed" at accessibility sizes. `DetailFactRow.labelWidthFor` now follows the text scale to a 2x cap, and past 45% of the row the label stacks above its value the way iOS settings rows do. **(h)** a floating snackbar still spans the window, so one line ran the full 1032 pt of an iPad. `BoardhopTheme.forWindow` caps it at 560 pt from `Breakpoint.medium` up, applied once in `MaterialApp.builder` because the width is read where the bar is shown and there is no per-call-site fix for all 21 of them; measured on the iPad at exactly 560 pt, centred. **(j)** the conversation composer was docked under all three PR tabs; the page now owns its `TabController` (rather than a `DefaultTabController`) so the scaffold rebuilds on a tab change, and the composer appears only on Comments — under Files a comment belongs to a line and is written from the diff gutter. **(k)** a failing policy that does not block the merge drew the same red X as a blocking one; `checkIcon`/`checkColor` now take `isBlocking`, an optional failure is an amber warning triangle, and the row says "optional" out loud instead of signalling only by the absence of "required". Unit-tested; **not demonstrated on a device** — no PR reachable from here has a failing optional policy, and the scratch project has no branch policies. **(m)** board drag-and-drop **works on iOS**: NEXT-STEPS had assumed idb could not long-press drag, but `idb ui drag-and-drop <from> <to> --press-duration 0.9 --duration 1.6` does exactly that. 15546 dragged out of New into Active (New went to 0, Doing to 7/5 over its WIP limit) and back again. The move back also raised the **412 conflict banner** ("changed elsewhere; the board was reloaded"), which was itself on the unexercised-on-iOS list and behaved correctly; a retry after the reload succeeded and the board is back as it was. The **overflow menu** opened 24.5 pt from the glass rail on the iPad in landscape, an opaque card hard against it; app-bar trailing menus now take `kTrailingMenuOffset` (`lib/theme/layout.dart`), measured at 48.5 pt after. Note Flutter applies roughly half the offset given. Three more defects found while checking, all fixed: the work item detail header overflowed **29 px** at xxxL (the type line now wraps); "Recent repositories" was character-wrapped under "See all" on the project home page (the action drops to its own line past 1.5x); and the earlier assumption that Kelly's Mac runs Xcode 26.6 is wrong — it is **Xcode 27.0** with the iOS 27.0 SDK. **Still open**: `html_editor_enhanced` prints a style map per keystroke in debug (in the package, not ours); ~~iPhone portrait keeps the Material bottom bar rather than a glass pill~~ **done 2026-09-12**: Kelly chose porting the rail over adopting `liquid_glass_widgets` 1.4.4 (reviewed: active and popular, but it has no side rail, its `GlassScaffold` replaces Material's and its glass needs isolation scopes outside it), so the iPhone in portrait now gets the same bottom glass bar as the iPad in portrait through `GlassShellLayout`, with the bar's items sharing its width at large text sizes; verified on the simulator (Home, Work, board, detail composer above the bar, dark, xxxL); the 412 banner, custom page tabs and board drag-and-drop remain unexercised on iOS. Tooling: `idb ui rotate --udid <udid> PORTRAIT|LANDSCAPE_LEFT|LANDSCAPE_RIGHT` rotates deterministically (the osascript recipe was not). Still to run on a physical iPhone: Authenticator broker, Conditional Access, notification delivery. Kelly's tablet feedback the same day: the Material rail's solid column cut into the app bar, so on iOS and macOS the shell now uses `GlassNavigationRail` (`lib/features/shared/widgets/`): a floating, blurred, translucent pill over the unchanged page background (`Radii.xl`), vertically centered at 80% of the height with the four destinations spread evenly along it (Kelly's second round of feedback), layered above the body in a Stack with a 24 dp margin so its shadow rests unclipped (third round), with vertical pages padded clear of it and sideways scrollers (the board, `ProjectShell._bleedsUnderRail`) given the gutter as left MediaQuery padding so columns rest clear and slide under the glass when scrolled (blur plus saturation boost, thin tint, specular top highlight; fourth and fifth rounds); in portrait the rail lies along the bottom at 80% of the width and pages get its height as bottom MediaQuery padding, so lists scroll under it and clear it at the end, Kanban cards included (sixth round); in landscape the rail sits on the right by default with a Settings > Appearance switch to move it left, persisted by `ThemeController` (seventh round), verified on the iPad in light and dark; Android keeps the Material rail. Wider single-page views (Kelly, same day): `ContentColumn` follows the window up to 1120 dp instead of stopping at 840, and Home, the repo page and the PR overview lay their sections side by side (`SideBySide`, from 960 dp of content width). iPhone in landscape (Kelly, same day): the glass rail showed as a 13 pt sliver and text ran under the Dynamic Island, because the rail's `SafeArea` sat inside its own 72 pt box (shrinking it by the 59 pt side inset instead of moving it) and no page applied the side insets at all. The Apple chrome now lives in `GlassShellLayout` (pure layout, `test/features/glass_shell_layout_test.dart` checks it at phone and tablet insets): the page keeps clear of the sides with a `SafeArea`, the rail sits outside those insets, the board keeps the insets plus the gutter as padding and slides under both, the rail is held to at least 80% of the safe height and grows only for larger text instead of overflowing, and the four pages outside the shell (organizations, projects, settings, diagnostics) wrap their body in `SafeArea(top: false, bottom: false)`. The Android rail row got the same SafeArea, not yet checked on a device. Verified on the iPhone 17 simulator in landscape (light, dark, xxxL text) and on the iPad in both orientations, unchanged. Same session, island on the left: the rail on the right then sat the full inset plus its margin from the edge (Kelly: way too far). iOS reports the same inset on both sides, so `DisplayCutout` (`lib/core`) asks the Runner for the interface orientation over a method channel in `AppDelegate.swift` (`com.kammcs.boardhop/display`, read again on every metrics change) and `GlassShellLayout` keeps only the margin from the island-free edge, clearing the inset only on the island's side or when the side is unknown (Android, tests). `tool/shot-ios.sh` gained `ROT=right` for that orientation. **iPhone dock, Apple's geometry (Kelly, 2026-09-12, from an App Store screenshot):** the bar floated far too high and highlighted the active tab the Material way. Measured off the screenshot (iPhone 17): Apple's floating tab bar is **56 pt tall**, its bottom edge **20 pt off the screen's bottom edge** (so it sits over the home-indicator band rather than clearing the 34 pt inset), **22 pt off each side**, a true pill. Ours was 68 pt tall and 58 pt off the bottom (24 pt margin on top of the inset) at 80% of the width. `GlassShellLayout` now takes `barBottomMargin` 20 with no bottom `SafeArea`, gives a phone a fixed `barSideMargin` 22 (a tablet keeps the 80% fraction, which is what Kelly approved for the iPad), and its `barGutter` is measured from the screen edge so it replaces the home-indicator inset instead of adding to it. `GlassNavigationRail`'s horizontal thickness is built from named parts that sum to 56, the bottom bar clips to `Radii.pill` (the side rail keeps `Radii.xl`; a pill's semicircular ends read wrong down a whole screen) and the destinations share the bar's width so the selected capsule is a tab-shaped stadium rather than the near-circular blob a fixed 72 pt slot gave. Active tab: a capsule of brighter glass behind the icon **and** label with a hairline and a soft shadow, replacing the 56x32 Material indicator pill behind the icon alone. The neutral slate theme's `primary` (#575f6b light) is all but the same value as `onSurfaceVariant` (#474648), so tinting the selected glyph carries no signal: the capsule plus weight does the work, selected at full-strength `onSurface` and semibold, unselected dimmed to 0.7. Two defects found and fixed on the way: a `Border` wrapping the item's content added itself to the rail's own size (72 became 80, thickness 56 became 58), so the capsule is painted behind the content in a `Stack`; and a `Stack` hands its non-positioned child loose constraints, so the width belongs on the content, not on the Stack, or every icon and label shrinks to its own size and pins to the left of its slot (covered by a centred-in-its-slot assertion in `test/features/glass_navigation_rail_test.dart`). Separately, a real pre-existing bug the lower bar exposed: a `ListView` that sets its own `padding` stops consuming the ambient `MediaQuery.padding`, so the last row of the project home page could not be reached past the glass bar (the hidden band was 94 pt before this change and 44 pt after). `scrollEndPadding(context)` in `lib/theme/layout.dart` keeps the page's own breathing room and grows to the inset when the inset is larger; applied at all fourteen `EdgeInsets.only(bottom: Spacing.xxl)` scroll views. Verified on the iPhone 17 simulator in light and dark (bar measured at 56 pt tall, 20 pt off the bottom, page end clearing it) and on the iPad Pro 13" in portrait and landscape, the side rail unchanged.
17. **TestFlight (2026-09-11, Kelly's ask; ships the way multipass does, see `../multipass-theater/docs/runbooks/RELEASE.md`).** Done on the Mac: Runner signs automatically on team **73W98CESN9** ("Kelly Kamm", Apple ID **syndalis@mac.com**), `ios/ExportOptions.plist` (app-store-connect, automatic, export, symbols), `ITSAppUsesNonExemptEncryption=false` in Info.plist (HTTPS only), and `tool/ship-ios.sh <build-number> [--skip-build]` which runs `flutter build ipa` with the `.env` defines, checks the exported entitlements, and uploads with `xcrun altool --upload-app` as syndalis@mac.com using the app-specific password (read at run time from the gitignored `.env.asc` as `BOARDHOP_ASC_PASSWORD`, else from the multipass repo's `.env`; never committed or printed). The first signed archive + App Store export succeeded (1.0.0 build 1, 41 MB): automatic provisioning registered `com.kammcs.boardhop` on the team and the store build carries `73W98CESN9.com.kammcs.boardhop` plus the `com.microsoft.adalcache` keychain group. Kelly created the App Store Connect record the same day: **BoardHop**, bundle com.kammcs.boardhop, Apple ID **6811138100**, SKU com.kammcs.boardhop. First upload 2026-09-11 13:59: 1.0.0 (1), delivery UUID 50b073db-3380-4479-87f3-bb6f6723b7a8, `tool/ship-ios.sh 1 --skip-build`. Build 1 came back with ITMS-90683: missing NSPhotoLibraryUsageDescription and NSCameraUsageDescription (required) and NSLocationWhenInUseUsageDescription (advisory), referenced by html_editor_enhanced's file picker and flutter_inappwebview even though the editor has picture/audio/video/file insertion off. Info.plist now carries purpose strings that say Boardhop does not use them and never prompts; build 2 uploaded (delivery UUID 00bcaa8e-230b-4da9-8bd7-e35831861fed). Build 3 (delivery UUID cd2947d3-d4a4-4a79-be8d-7850feaf922d, 2026-09-11, `tool/ship-ios.sh 3`) carries the landscape safe-area and rail fixes; it was VALID and in internal testing within minutes, auto-distributed to the group. Build 4 (delivery UUID 5d88b499-9203-4d16-882c-a4aa69d0c618, 2026-09-12) carries the Windows session's work item forms, the second walkthrough's fixes and the iPhone glass bar. Kelly made the internal group "BoardHop Testers" and joined it after build 2 had landed; App Store Connect left the tester at NOT_INVITED with no resend option and no mail. Kelly then added an **Admin App Store Connect API key** (`AuthKey_YD5P39439T.p8` at the repo root, gitignored via `*.p8`; issuer id = the team's public id); `POST betaTesterInvitations` through it sent the invitation (state INVITED). `tool/testflight.rb status | invite <email> | notify` wraps those calls for next time. Later, before external testers or review: App Privacy answers, a `PrivacyInfo.xcprivacy` for Runner, screenshots and the listing. **Google Play (2026-09-14, Mac):** Kelly created the Play Console app record `com.kammcs.boardhop` with Play App Signing and invited the Play Developer API service account (JSON at `android/keystore/play-service-account.json`, gitignored; Play Console no longer has an API-access page, the user invite is the whole link). The upload key is a 4096-bit RSA keystore at `android/keystore/boardhop-upload.jks` with passwords in `android/keystore.properties` (both gitignored; **Kelly backs both up off the Mac**, the passphrase exists nowhere else), read by `android/app/build.gradle.kts` as the `upload` signing config; `bundleRelease` refuses to run without it instead of falling back to the debug key. `tool/ship-android.sh <build-number> [--skip-build] [--track <track>]` builds the AAB with the `.env` defines and uploads it with fastlane supply (`android/fastlane/`); build numbers share the counter with iOS. **First upload done: 1.0.0 (3) on the internal track.** The upload key's MSAL hash is `84xsSJcgEyk8T+PmW/DilFwYqFo=` (`msauth://com.kammcs.boardhop/84xsSJcgEyk8T%2BPmW%2FDilFwYqFo%3D`, in `secret.properties` as `MSAL_RELEASE_SIGNATURE_HASH`). Build 3 on the Pixel 10 **stalled on the launch screen**: logcat showed MSAL's `redirect_uri_validation_error`, because the manifest carried the upload key's hash and Dart's `AppConfig.androidRedirectUri` its debug default while the installed app is signed with Google's app signing key. Fixed the same day: `MSAL_RELEASE_SIGNATURE_HASH` is now Play's certificate hash `Y5+yspRtdI7mowXCrmOMq11MoLI=` (read off the installed `base.apk` with `apksigner`, research/09 A5), `tool/ship-android.sh` passes the matching `BOARDHOP_ANDROID_REDIRECT_URI` define to Dart, and `main.dart` shows `StartupFailureApp` with the error instead of a frozen splash. **Blocked on Kelly:** register `msauth://com.kammcs.boardhop/Y5%2ByspRtdI7mowXCrmOMq11MoLI%3D` on the app registration (the upload-key URI is only needed for side-loaded release APKs, which cannot sign in anyway), update to build 4 from the internal track, sign in once. Later: internal testers list, data-safety form, privacy policy URL, listing and screenshots (phone, 7" and 10" tablets). **Diagnostics hidden in store builds (2026-09-14, Kelly):** `AppConfig.diagnosticsEnabled` (`!kReleaseMode`, or `--dart-define=BOARDHOP_DIAGNOSTICS=true`) gates the bug icon, the `/diagnostics` routes and, from Play 8 / TestFlight 9, the Activity feed's push registration row with Send test (`ActivityPage.showPushStatus`); Play build 7 and TestFlight build 8 were the first without the Diagnostics page. **TestFlight 8 on Kelly's iPhone registered a production token and received the test push through the extension; the relay pruned the debug build's sandbox token (`BadDeviceToken` → removed).** **TestFlight 1.0.0 (8) uploaded 2026-09-14** (`tool/ship-ios.sh 8`): the first App Store export with the Notification Service Extension embedded; automatic signing produced the second bundle id's profile without a portal visit, and the exported appex carries the app group and the `adalcache` keychain group. **Relay gap found by this export:** the exported Runner carries `aps-environment: production`, so a TestFlight install's APNs token belongs to the **production** environment while the relay still sends to the sandbox (`APNS_ENV=sandbox`, right for the debug build on Kelly's iPhone). Apple answers `400 BadDeviceToken` for a token sent to the wrong host, and the relay then marks the device dead. **Decided 2026-09-14 (Kelly): the relay is on `APNS_ENV=production`** (`relay.env` on the box, `docker compose up -d --force-recreate relay`, healthz `apns: ready (production)`). TestFlight and App Store installs get pushes; a debug build run from Xcode or `flutter run` on a phone carries a sandbox token and is marked dead on its first send, so from now on push checks on iOS use the TestFlight build, and the extension's breadcrumb file is still readable with `devicectl` from a TestFlight install (same app group). A sandbox fallback on `BadDeviceToken` stays an option if a debug build ever needs pushes again. **Store state (2026-09-14 evening, Kelly's ask):** build **13** (1.0.0, 2026-09-15) on the Play internal track and in TestFlight (10 carried mentions, the work item tabs, the counted tab strip and the keyboard fixes; 11 the composer riding the keyboard on the pull request page; 12 comment attachments; 13 sprints and the board's offline fix); the next upload on either store is 14.
14. **Work item forms (planned with Kelly 2026-09-12; phases 0–5 complete).** Research and plan in `research/11-work-item-forms.md`: the app cannot create work items, and edits stop at title and description. Agreed design: a `+` left of the Items/Board pill (also a `+` per board column with state and lane pre-filled, and "Add child" from a work item), a type chooser with backlog types first and the rest under "Other" plus team templates, a form engine built from the Processes layout API (pages, sections, groups, controls, custom fields in web order) with a field-list fallback, controls per field type (picklist, identity picker from the type's allowed values, area and iteration tree pickers, tags, dates, rich text through the existing editor with a Markdown choice), state from the type's transitions, `validateOnly` dry runs with errors under their fields, then create. Phone: full-screen page with a pinned header and stacked collapsible groups; tablet: centered web-like dialog up to 960 dp with the sections in columns and page tabs. The same form replaces the edit page. Online only with local drafts. Links and attachments in the last phase. Spikes run the same day (s24, s25, s26 read-only; w16, w17, w18 in the scratch project; research/11 §9): the Processes layout API refuses every stock type (VS403115), but the type's `xmlForm` carries the full form for every type, so the engine parses that; a `validateOnly` create returns the server's defaults, so the form opens from a dry run; a new item can only start in its initial state, so a board-column create is create-then-patch; parent links, tags, Markdown and attachments all ride in the create call (#15539, #15540, #15541 created). The Assigned To allowed-values list is empty in both projects, so the people picker is now team members (cached, recent first) plus a project-scoped Graph subject query as you type (Kelly, same day). **Phase 0 landed 2026-09-12** (no UI yet): `lib/data/models/work_item_form.dart` (`FieldSpec`, `FormLayout` parsed from `xmlForm` through the new `xml` dependency, `FormSpec`, `ClassificationNode`, `TeamDefaults`, `WorkItemTemplate`, `BacklogTypes`, `ValidationError` / `WorkItemRuleException`, `WorkItemDraft`; `WorkItemType` gained `transitions` and the parsed layout and drops the raw XML) and `lib/data/repositories/work_item_form_repository.dart` (`formSpec` with the field-list fallback, classification nodes, team defaults, tags, templates, backlog types, team members and the Graph people search, `validate` / `create` / `validatePatch`, attachment upload, link-target search, drafts, and the pure patch builders), all cached 24 h in `cache_entries` under account-namespaced `form:*` keys. Fixtures for the tests come from the read-only spike `s27_form_fixtures.py` (scratch Bug and User Story) in `test/fixtures/forms/`; 57 new unit tests in `test/data/work_item_form_*_test.dart` and `work_item_draft_test.dart`. Spike `s28` confirmed `graph/storagekeys/{descriptor}` resolves a Graph descriptor to the identity id, but spike **s29** (2026-09-12, read-only) then found that the work item store **refuses that id** for `System.AssignedTo` ("unknown identity"), so `assignedToValue` now sends `"Display Name <unique>"` and keeps the id only for an identity without a unique name. **The detail page is layout-driven since 2026-09-12** (Kelly reported that #15303's custom fields were invisible): after the header and the facts rows it renders the type's own Details groups in web order (`detailGroupsFor`, the same `pageViewsFor` the form uses, filtered to the fields the item has a value for), html controls as rich text and everything else as label → value rows, panels and the header's own fields left out, with the old fixed long-text list as the fallback until the spec arrives; verified read-only on CloudCover 2.0 #15303 on the phone and the tablet (Testing Plan and Classification now show; spike s38 found QA Assignee and QA Story Points are genuinely **empty** on that item, so they only appear in its edit form, which was opened and closed without saving) and on Bug #3898, where QA Assignee and QA Story Points do carry values and render as an identity with its avatar and a trimmed number; the scratch project's #15546 gained Story Points and Value area and lost nothing.

    **Phase 1 landed 2026-09-12** (type chooser, the `+` in the Work app bar, the phone create form). `lib/features/work_items/form/`: `new_work_item_button.dart` (a `+` immediately left of the Items/Board pill on the work items page and the board, icon only on compact and icon + "New" from medium up, which reloads the list or board when the form closes); `type_chooser.dart` (bottom sheet on a phone, anchored menu from medium up; backlog levels top-down — a level the team hid still offers its type, only `Microsoft.HiddenCategory` hides one — then a collapsed "Other", with the project's last used type pinned under "Recent" and a type's templates expanding to "Blank" plus the names when the team has any); `work_item_form_page.dart` (route `…/work-items/new?type=&team=&state=&lane=&laneField=&parent=&template=`, placed before `:id` in the router; loads the spec, the team defaults and a `validateOnly` dry run whose fields become the initial values under the pre-fills; a parent copies its area and iteration and rides along as a `Hierarchy-Reverse` relation; `AdoAuthException` → `AuthInteractionRequired`, other failures inline with Retry); `work_item_form_state.dart` (values, dirty set, per-field errors, the 800 ms debounced dry run on a field with `dependentFields`, and the patch through phase 0's `buildCreateOps`); `work_item_form_body.dart` (pinned header: type chip, Title, then State — read-only on create, in its state-category colour — Assigned to, Area, Iteration and Tags chips; below it one card per labeled group of the Details page in web order with every column flattened top-down, groups of links/attachments/deployments panels shown as a disabled "Available after creation" card, custom pages under their own heading); `controls/` (text/number, allowed-value sheet with a free-text row where the field is not a declared picklist, date, switch, people picker with "Me" and "Unassigned" pinned plus team members and a 300 ms-debounced Graph search, area/iteration tree picker with the team's iterations first and the current one marked, tag chips with suggestions); `form_prefs.dart` (last type and recent assignees per project in `shared_preferences`). 14 new widget tests in `test/features/work_item_form_test.dart` and `type_chooser_test.dart` (215 in total). Verified on the phone emulator against the scratch project: chooser order, the form opening on the server's defaults (Area, Iteration 1, Priority 2), the people and tree pickers, tags, an empty Title blocked locally, a free-text Priority of 9 refused by the server under its own field with the form still editable, **Task #15542 created** (assigned to Kelly, Iteration 1, tag `phase1`, Priority 3, read back through the API) and the snackbar's **Open** landing on #15545. Not verified: templates (no team has any), a board or parent pre-fill (phase 4 sends them), the tablet arrangement (phase 2), and the html field is still a plain multi-line box that is stored as one escaped `<div>` until phase 2 brings the rich editor. Next: phase 2 (tablet dialog, rich text, Markdown choice). **Phase 2 landed 2026-09-12**: from medium up the `+` shows the form as a centered dialog (up to 960 dp, 90% of the height) over the live Work list or board, with the header on two rows and the layout's own sections as columns from `FormSection.percentWidth` (50/50 on the stock types, 60/40 for three, a fourth spanning beneath), custom pages as tabs across the layout area, and the phone arrangement inside the dialog when it is narrower than 640 dp; the `…/work-items/new` route renders the same box centered on its own page so a deep link still works (the route sits inside the Work branch, so a real scrim over the list is only possible from the `+`, which uses `showDialog(useRootNavigator: false)` to stay inside the account's repositories). `html_editor_enhanced` moved into a shared `HtmlFieldEditor` (`lib/features/work_items/widgets/`) used by both `WorkItemEditPage` and the new `controls/rich_text_control.dart`: an html field is now a card with the rendered value and a pencil that opens the editor full screen on a phone and as a large dialog on a tablet (Cancel/Done in the app bar), and `System.Description` on a new item offers a Rich text / Markdown toggle (monospace box with a Preview toggle, the format op on create, the choice remembered per project in `FormPrefs`, no conversion between the two ever). Editor content now goes into the patch verbatim (`setRichValue`), while a long-text field typed as plain text is still escaped into one `<div>`. Picklists drop an anchored `MenuAnchor` from medium up and the people and tree pickers open as centered dialogs. Three phase 1 review fixes: the people search is now scoped with the project **id** (`graph/descriptors/{name}` answers HTTP 400, so the query had silently gone org-wide — spike s30), the iteration picker's Team section lists every iteration of the team (`teamIterations`, cached 24 h, current marked) and the same person no longer appears twice in the people list. 8 new tests (223 in total), `flutter analyze` clean. Verified on the tablet emulator (dialog over the list in dark and light, two columns, anchored Priority menu, people dialog with a project-scoped Graph hit, iteration picker with Iterations 1-3, the phone arrangement inside the dialog at a 650 dp window) and on the phone (full-screen HTML editor with bold, Markdown toggle and preview): **User Story #15546** (tablet, HTML description `<p>… <b>bold</b></p>`), **Bug #15547** (phone, HTML repro steps) and **Task #15548** (phone, `multilineFieldsFormat` **markdown**, `**bold** list`), all read back through spike s31 and rendering on the detail page. Not verified then: the page tabs (no process in reach has a custom page, so they are covered by a widget test only). The iPad simulator followed on 2026-09-12 (step 16, second walkthrough): the dialog, columns, menus and pickers all behave. Next: phase 3 (edit through the same form, transition-driven state sheet). **Phase 3 landed 2026-09-12**: the pencil on a work item now opens the **same full form in edit mode** — `WorkItemFormPage.edit` on the route `…/work-items/:id/edit` (a dialog over the detail page from medium up, full screen on a phone), loaded from the item itself with every control the layout has, read-only fields shown as text when they carry a value, the long text kept in its own format and never re-escaped, an editable **State chip over the type's legal transitions** that reveals **Reason** under it when the state moves, Save disabled until something changes, `validatePatch` then `patch` with only the changed fields and `test /rev`, rule errors under their fields, and an **HTTP 412 conflict banner** with Reload (keeps the typed values on top of the fresh copy) and Discard; success pops back to the detail page, which refreshes and says "Saved". The form stays online-only: with no connection the cached item opens read-only behind "Editing needs a connection" with a Retry, while the detail page's quick actions (state, assign, comment) keep the write queue. `WorkItemEditPage` is deleted (`HtmlFieldEditor` stays); the detail page's state sheet lists `transitionsFrom(state)` — free, because the cached type list already carries `transitions` (spike **s32**) — and asks for a Reason in a second step where a process marks it required (no stock type does). Two phase 2 review fixes: re-tapping the current tab no longer throws an open form away (the new `UnsavedWork` registry lets `ProjectShell` ask with the form's own discard dialog, and a re-tap at the branch root does nothing), and the `+` no longer stays disabled after the form closes. 12 new tests in `test/features/work_item_form_edit_test.dart` (**235** in total), `flutter analyze` clean. Verified on the phone emulator against scratch items: **#15542** edited (title + " (edited)", Priority 1, New → Active, the server chose Reason "Work started"), **#15543** given a real 412 by spike `w19_bump_rev.py` — banner, Reload kept the typed title, Save succeeded on the new revision — the tab re-tap prompt, and the offline banner with Retry under airplane mode; **#15544**'s state sheet on the detail page; and on the tablet emulator **#15546** edited in the dialog (Story Points 5). All read back through spike `s33`. Not verified: a process that marks Reason required, a read-only field on a card (no stock type has one — widget test only), and the iPad simulator. Next: phase 4 (board column `+`, "Add child", templates, drafts). **Phase 4 landed 2026-09-12** (board column `+`, Add child / Add related, templates, drafts). Every board column now ends in a quiet `+ New item` row (`lib/features/boards/widgets/new_card_row.dart` through the new `KanbanBoard.columnFooterBuilder`; a split column shows it in its Doing half only) which opens the form with the column's `stateMappings[type]`, the lane on screen (the board's `fields.rowField` plus the selected lane, skipped for All lanes and the default lane) and **the board's team** passed through the route as `team=`; the board's types are the only ones offered, and a board with a single type skips the chooser. Save is **create then patch** (spike w18: only the initial state is legal on create): the create carries everything but State, then `boardFollowUpOps` writes State (and the lane field, which the layout does not carry) in one `test /rev` patch; a failed second call keeps the item and says so — "#N created in New (could not move to Active)" with **Open** — and the board reloads either way, tinting the new card for 1.8 s (`WorkItemCard.flash`). The detail page gained an overflow with **Add child** and **Add related** (`…/work-items/new?parent=15546&rel=child|related`): the child's type is the backlog level below the parent's (`BacklogTypes.childTypeNames`, Bug follows `bugsBehavior` because the service already places it), the form pre-fills the parent link, Area and Iteration and shows "Child of #15546 - <title>" under the type chip, and the page now lists a compact **Links** row (parent and children with titles through one `batch`; `WorkItem` keeps the `relations[]` of the `$expand=all` read, which it used to drop). Spike **w20** created the first team template — **Boardhop spike task** (Task, id `edae8a24-9850-401b-9d0c-7df212025c56`, left in place on the DevOps Mobile App Team) — so the chooser's template expansion is exercised at last: the list read carries no `fields`, so "Blank" plus the names come from the list and the values from `templates/{id}`; a stale **empty** template list is now re-read once per session (the day-old empty copy hid the new template) and the title keeps a template's trailing space with the caret after it. **Drafts** (create only): closing a dirty form asks Keep draft / Discard / Cancel and a network failure offers Keep draft in its banner; the draft keeps the values the patch would send plus the formats, relations and pre-fills, the chooser lists `Resume draft: Task - Half typed - 5m ago` with a delete icon, resuming marks everything dirty (and long text rich, so it is never escaped twice), a successful create clears it and one older than 30 days is dropped on read. 26 new tests (`board_new_item_test.dart`, `child_type_test.dart`, `draft_test.dart`, plus the chooser and form suites), **261** in total, `flutter analyze` clean. Verified on the phone emulator against the scratch project: the Active column's `+` created **User Story #15549** (the Stories board offers User Story only, so no chooser; the card landed in Active/Doing at 2 / 5 after the follow-up patch), **Add child** on #15546 offered Task and Bug and created Task **#15550**, whose parent's Links row then reads "Children (1) #15550"; the Work `+` then showed **Blank + "Boardhop spike task"** under Task and the template filled the form (title `"[template] "` with the caret after the space, Priority 1, tags template/boardhop) into Task **#15551**; and a dirty form closed with **Keep draft** came back as "Resume draft: Task - [phase4] draft resumed - just now", resumed dirty, created Task **#15552** and left the chooser without a draft row. Everything read back through the read-only spike `s34_phase4_items.py`: #15549 rev 2 Active with `Kanban.Column` Active and `Column.Done` false (the service derived the column from the state, spike w02) and the process rule assigned it to Kelly, #15550 carries `Hierarchy-Reverse` to 15546 while 15546 carries `Hierarchy-Forward` to 15550, #15551 has Priority 1 and tags `boardhop; template`. On the tablet emulator: the `+ New item` row in every column but a split column's Done half, the form as a dialog over the live board from the New column's `+` (state chip New) and **Add child** from #15549's overflow, whose dialog reads "Child of #15549 - [phase4] from the Active column" (nothing created from the tablet). Not verified: an item created from a lane-filtered column (the scratch lanes hold the two older stories), a failed follow-up patch (the snackbar path is covered by a test only) and the iPad simulator. **Phase 5 landed 2026-09-12** (Links page, Attachments page, Insert image). Every work item type's layout carries a Links and an Attachments tab, and both are now real: `pageViewsFor` appends them after Details and the custom pages (a section at the bottom on a phone, a tab on a tablet), and the three disabled “Available after creation” cards are gone. The **Links** panel (`form/controls/links_section.dart`) groups the item's links by kind — Parent, Children, Related, Predecessor, Successor, Duplicate, Duplicate of and an **Other** bucket, mapped locally rather than through `wit/workitemrelationtypes` so it works from the cache — resolves each row's title, type and state through one `batch` (an unreadable target shows as its id), removes by swipe or the trailing ×, and adds through a sheet with a kind picker and a search where an id typed exactly resolves to that item and text searches titles (Child and Related multi-select). The **Development** and **Deployment** groups of the Details page show “Managed in Azure DevOps” instead, told apart by the layout's own `WorkItemLinkFilters FilterType="excludeAll"` rather than by their names; **Related Work** is a real list. The **Attachments** panel (`form/controls/attachments_section.dart`) lists each `AttachedFile` relation with its name, size (`attributes.resourceSize`) and comment, renders image thumbnails through the bearer token (cached by guid in memory and in the app support directory), opens an image full screen in an `InteractiveViewer` and any other file through the share sheet after downloading it, and adds from **Take photo / Choose from library / Choose file** with the documented 60 MB cap checked before the file is ever read. On create both ride in the create patch; on an existing item a link waits for Save while an **attachment is written at once**, so an upload is never orphaned. The rich editor gained an **Insert image** button at the end of its own toolbar (`customToolbarButtons` took it cleanly) which uploads, adds the `AttachedFile` relation and pastes `<img src=…>` at the caret; the image only renders after Done, because Summernote's WebView sends no Authorization header, while the control's preview and the detail page render it properly. The detail page's Links row now shows **every** link kind and gained an Attachments count. New dependencies: **`image_picker` 1.2.3** and **`file_picker` 10.3.10**, free, open source and pinned; no Android permission is needed (system photo picker + SAF), only the optional camera feature, and the iOS camera and photo-library usage strings were corrected from “Boardhop does not use…” to what it actually does. Three API facts came out of it (spikes **s35–s37**, read-only, recorded in research/01 §2.6): a **relation-only patch creates no new revision** (adding and removing a link left `rev` 1 and `ChangedDate` untouched, while an attachment bumped both), so `test /rev` is no guard and the save **re-reads the item** and takes the positional removal indices from that copy, in descending order; **`System.AttachedFileCount`, `RelatedLinkCount` and `ExternalLinkCount` are not returned by the item read at all**, not even with `$expand=all`, so the count comes from the relations; and the service **rewrites a relation's URL with the project GUID**, so a relation is identified by its target id or attachment guid, never by the URL the app sent — without that, an attachment written immediately stayed pending and the next Save was refused with “Relation already exists”. `WorkItemRepository.patch` now asks for `$expand=relations` so a patch stops dropping the cached relations. 44 new tests (`links_section_test.dart`, `attachments_section_test.dart`, `buildRelationOps` in `work_item_form_ops_test.dart`), **305** in total, `flutter analyze` clean. Verified on the phone emulator against the scratch project: **#15550** given a Related link to #15547 through the sheet and saved (read back on the server, then removed again and read back gone), **#15540**'s existing PNG thumbnail rendered and a second image uploaded from the library and attached at once (rev 2, `resourceSize` 2914), and **Add child** on #15546 created Task **#15553** carrying the parent link, a Related link to #15547 and an uploaded image **in one create call**, whose detail page reads “Attachments 1” with both links; the full-screen viewer, the Insert image button and the embedded image in the description preview were all exercised. Everything read back through the read-only spikes `s35`–`s37`. On the tablet emulator the edit dialog for **#15540** now carries **Links** and **Attachments** tabs (both thumbnails rendering) and its Details page shows “Managed in Azure DevOps” for Deployment and Development beside a real Related Work list; nothing was written from the tablet. The new pages were also checked in the **light** theme and the emulator's Dark setting put back as Kelly had it. **Phases 0–5 are complete.** What is still unverified: the **iPad simulator** at any phase; a process that marks **Reason** required and a **custom page tab** (no stock type has either, so both are widget tests only); a **Hosted XML** project, where the `xmlForm` path and the field-list fallback are untested; a **read-only field on a card**; a create from a **lane-filtered** board column; and the **camera** source of the attachment picker (the emulator has no usable camera) together with the Authenticator broker and Conditional Access paths, which still need a physical device.
15. **DartNative and the iPhone Duo (evaluated 2026-09-12, Kelly's question).** `research/12-dartnative-evaluation.md` (verdict) with `12a` and `12b` (sourced findings): DartNative is a 43-day-old closed-source preview with no test framework, no WebView, no drag-and-drop, no slivers and no iPhone Duo statement; a move would rewrite about 27,000 lines and reopen the rich text, Kanban and diff decisions. **Decision: stay on Flutter; revisit mid-2027 at the earliest.** For the Duo (ships 2026-10-23 on iOS 27.1; inner display is regular x regular, Split View is a fixed 50/50): rebuild against the Xcode 27.1 SDK when it ships, lower `SideBySide.twoColumnMin` to about 880 so the inner display gets two columns (`ContentColumn` yields 903 there), degrade `DisplayCutout` to unknown on the Duo and move it to a `reservedRegions` query, test the WebView editor under a live Split View resize, watch flutter/flutter#192515. **Kelly approved both code items 2026-09-13 and both are done.** (1) `ContentColumn.twoColumnMin` is **880**, not 960, so the inner display's 903 pt of content gets two columns; a phone (402) and an iPad Pro 11" in portrait (834) still stack. (2) `DisplayCutout` grew into a reserved-regions query: `DisplayCutout.regions()` returns a `DisplayRegions` with typed `ReservedRegion`s (`division` for a hinge, `occlusion` for a camera housing, `unknown` for anything new so a kind Apple adds later cannot silently become "nothing in the way"), a `hinge` rect, a `folds` flag and a `supported` flag that separates "asked, nothing reserved" from "never asked"; `side()` now answers `CutoutSide.unknown` whenever a division is reported, because on a folding display the interface orientation no longer says where the housing is. Seven tests in `test/core/display_cutout_test.dart` drive it through a mocked channel. **The Swift half is plumbed but unverified:** `reservedRegions(kind:)` is an iOS **27.1** API and this Mac has Xcode **27.0** with the iOS 27.0 SDK, whose UIKit headers carry no such symbol and which ships no iPhone Duo simulator device type (both checked 2026-09-13). A typed call therefore cannot compile, and `#available` does not help with that — it is a runtime check, not a compile-time one — so `AppDelegate.swift` gates on `#available(iOS 27.1, *)` and then reaches the method by selector, returning an empty list when it is absent. The selector spelling is from Tech Talk 111461 and is a guess until the headers land. **When Xcode 27.1 arrives: check the spelling, replace the selector with the typed call inside the existing guard, and run the Duo simulator questions in research/12b.**
16b. **iPhone Duo reserved regions in Flutter (agreed 2026-09-12; Mac, needs the Xcode 27.1 beta with the iPhone Duo simulator).** Apple's reserved regions are a UIKit view API (`view.reservedRegions(kind:)`, kinds `.division` for the hinge and `.occlusion` for the camera, `.includeInactive` for inactive ones; Tech Talk 111461, spellings provisional until the headers land), and the whole Flutter UI is one `UIView` under `FlutterViewController`, so the Runner can query it and hand rectangles to Dart through the existing `com.kammcs.boardhop/display` channel in `ios/Runner/AppDelegate.swift`. Steps: (1) install Xcode 27.1 beta, build against the 27.1 SDK (Flutter 3.47 already did the `UIScene` migration; confirm the app launches edge to edge on the Duo simulator, i.e. tier 3 needs no more than the rebuild); (2) in AppDelegate add a `reservedRegions` method behind `if #available(iOS 27.1, *)` returning `[{kind, x, y, width, height, active}]` in the Flutter view's coordinates (try the Flutter view first; if callbacks only reach the window's root view, query `window.rootViewController.view`), and forward the region-change callback to Dart the way metrics changes are already re-read; keep the orientation answer for older iOS; (3) grow `lib/core/display_cutout.dart` into a `DisplayRegions` model (hinge rect or none, occlusions, derived cutout side) exposed through an inherited widget next to `Breakpoint`, with `MediaQuery.displayFeatures` as the fallback source if Flutter ever populates it on iOS (flutter/flutter#192515); (4) apply the hinge: `SideBySide` puts its column gap on the hinge, `GlassShellLayout` keeps the rail off it, dialogs (work item form, pickers) center on one half, the Kanban board treats it as a snap point; lower `SideBySide.twoColumnMin` to about 880 so the inner display (content width 903) gets two columns; (5) walk the app on the Duo simulator folded, unfolded, rotated and in Split View (the WebView editor under a live resize first), and answer the ten simulator questions in `research/12b-iphone-duo-and-flutter.md` appendix (divider draggable or not, exact point sizes, how `MediaQuery` reports the fold); (6) write the answers into research/12 and update DESIGN.md with the hinge rule. Effort: about a day for the channel and model, one or two for the layouts.
18. **In parallel, administrative:** start Partner Center enrollment; register the boardhop domain; Apple/Google developer accounts.
19. **Push notifications and live logs: extension, relay, gateway (design revised 2026-09-13, Kelly).** research/06 now has two tiers: a kammcs-hosted push gateway (the only holder of the APNs key and FCM service account, pointer-only contract) serving every customer, and a multi-tenant relay that is shared on the kammcs server by default and customer-hosted (same container, API key per instance) as an enterprise option. Expo is gone. **Beta with puremedia needs no Microsoft business relationship:** free Marketplace publisher, extension published private and shared with the `puremedia` org (their PCA installs it), relay plus gateway as one container behind Caddy on the kammcs Hetzner box, and a written OK from puremedia that hook metadata (ids, links, event types, sometimes a title) lands on that server. **Capture endpoint live** at `https://boardhop.relay.kammcs.com` (relay skeleton in `relay/`, Caddy + Dart shelf, deployed 2026-09-13) and spike 3 done: **Minimal payloads carry only ids, so the relay cannot route them to people** (research/06 "The audience problem"); **Decided 2026-09-13 (research/06 "Decision"):** hooks send All, the relay keeps only metadata and emits a pointer with actor, verb, artifact and deep link; notifications must be meaningful and deep-link into the artifact; the device enriches before display with the user's own token (iOS Notification Service Extension with `mutable-content`, Android data-only HIGH-priority message handled in `BoardhopMessagingService`), falling back to the pointer line; no content snippets through the gateway. Service principal (s40) stays the product path for returning hooks to Minimal. **R2 designed 2026-09-13 (`research/14-notification-events-and-deep-links.md`, Kelly's decisions D1–D10 in its §8):** thirteen subscriptions per project (four `git.pullrequest.updated` filtered by `notificationType`, `merged` filtered to `Unsuccessful`, `run-state-changed` kept only to learn the requester identity for approvals), an event → audience → verb → collapse key → route table per family, pointer v2 (`actor`, `actorId`, `verb` enum, `detail`, `anchor`, `runId`, `subId`, `sentAt`), device enrichment per verb with an 8 s cap and the fallback line, Android private lock-screen version, relay-side prefs per `(org, userId)` with quiet hours (approvals exempt), dedup and fan-out rules (actor never notified except for builds and sole approvers; comment noise on `workitem.updated` dropped; one notification per person per event; 24 h delivery dedup on `X-VSS-ActivityId`), group reviewers/approvers skipped and disclosed. **R2.0 spikes done 2026-09-13** (s41, w24 with scratch PR 8348, w25 run 20260913.1; payload facts in `research/spikes/results/README.md`, every shape verified after Kelly approved the run's Deploy stage from the app; only a PR validation build's `triggerInfo` remains unseen because the scratch repo has no build policy). **R2.1 landed 2026-09-13** (0202d05): `POST /hooks/{org}` with per-org basic auth behind one 404 wall, `hook_subscriptions` registry with admin routes, 24 h dedup on `X-VSS-ActivityId`, bounded fast-200 `HookQueue`, typed `RoutingView` (mention GUIDs, thread id, comment-noise classifier), body-canary redaction test. **R2.2 landed the same day**: `lib/src/routing/` rule engine — verbs with priorities, per-family audience rules for every research/14 §2 row, `pr_state`/`pr_thread_state`/`run_state`/`build_state`/`projects`/`notification_sends` tables (90-day expiry), actor drop with the build and sole-approver exceptions, one notification per person per event, `UserPrefs` interface with the §6 defaults, quiet hours, 50-per-event and 60-per-user-per-hour caps, `NotificationSink` (logging only until R2.3); 229 relay tests. Review notes in research/14 §8a. **R2.3 landed 2026-09-13** (1ac4c2b, deployed to the box): pointer v2 (`actor`, `actorId`, `verb`, `detail`, `anchor`, `runId`, `subId`, `sentAt`, org-scoped `collapseKey`), APNs `mutable-content` + `thread-id` + category `boardhop.pointer`, FCM **data-only** HIGH with the artifact family as `collapse_key`, `user_prefs` with `GET/PUT /v1/prefs` behind the user's own token, `tzOffsetMinutes` on registration, `GatewayNotificationSink`; 286 relay tests. **R2.4 landed the same day**: app pointer v2 and verb phrases, anchors carried as query strings, Android data-only delivery posted by the app in the foreground and from a `@pragma('vm:entry-point')` background isolate (tag = collapse key, group = `org.family`, tap payload = the pointer), Settings → Notifications Push section per registered org (all §6 rows, optimistic writes, `notActor` fixed line), pushed pointers inserted into the Activity feed with poll dedup; 449 app tests. **Verified on the Pixel 10 Pro emulator:** the Push section in light and dark, one prefs write round-trip against the live relay, and a **data-only test push posted from the background isolate with the app backgrounded** ("Boardhop / Push is working" in the shade, tag `puremedia.build.0`, group `puremedia.build`) whose tap opened the Activity feed. Finding on the way (pre-existing, not fixed): when every feed source fails on a transient network error the feed shows empty instead of keeping the cached rows; it refilled on the next poll. **R2.8 landed 2026-09-13** (a870e88 + 6023229): `relay/tool/hooks.dart` (plan / secret / create / list / delete, guarded by `HOOKS_ALLOW_PROJECT`) run through `research/spikes/w26_provision_hooks.py`; the scratch project has **14 live subscriptions** posting to `/hooks/puremedia` and the relay routed the first real events. The first live assignment exposed that **work item identity fields carry an `id` only at resourceVersion 5.1-preview.3** (1.0 sends strings, 3.1-preview.3 objects without an id; spikes w27/w28), so the three work item subscriptions use that version. **R2.5 landed** (76e1e16): `?comment=`, `?tab=`/`?thread=`, `?tab=approvals&approval=&run=` reach the pages, scroll and tint the target (`AnchorHighlight`), a file thread opens the diff, a decided approval says "Already decided · Open run"; verified on the emulator through a debug-only route box on Diagnostics. **R2.6 landed** (8cc913a): `BoardhopMessagingService` enriches background pointers in Kotlin — silent MSAL token from the plugin's own `msal_config.json`, one or two GETs per verb family in an 8 s budget, `NotificationCompat` with BigText, group, tag = collapse key, the Dart notification id reproduced, `VISIBILITY_PRIVATE` with the fallback line as `publicVersion`, content intent into the existing push channel; verified for six verb families via a debug-only Diagnostics hook, and a real background FCM test push took the Kotlin path (`onMessageReceived` confirmed). Not yet verified: the lock-screen public version visually (no secure lock on the emulator) and the 3+ group summary. **Validated end to end on 2026-09-13 with two approved runs of pipeline 139 (`research/spikes/w29_validation_run.py`):** run 20260913.2 (`fail=true`) → the relay routed `build.complete` to one recipient, FCM delivered, and the Kotlin path posted "boardhop-scratch · 20260913.2 / Kelly Kamm queued a build that failed · Build › Fail on purpose: Bash exited with code '1'." with the fallback line as the lock-screen version; run 20260913.3 → `approval-pending` pushed "boardhop-scratch → Deploy / Needs your approval · Boardhop approval test: approve from the app. · 20260913.3", its tap opened Pipelines → Approvals on the pending card, the approval was granted from the card, `approval.completed` was correctly dropped (actor), and `build.complete` then pushed "queued a build that is fixed · Succeeded in 3m 41s" (`buildFixed` from `build_state`). Every push also went to the iPhone's stale APNs token (the debug build was uninstalled); APNs answers 200 until the token expires. Polish left for later: the build verb phrases read "queued a build that failed/is fixed" (rephrase to "Build failed"/"Build fixed" once the app localises verbs), and a pointer handled by Kotlin reaches the Activity feed only when tapped (the poll fills it otherwise). **R2.7 built 2026-09-13 (Mac), device acceptance pending:** `ios/BoardhopNotificationService/` is a `UNNotificationServiceExtension` target (bundle `com.kammcs.boardhop.BoardhopNotificationService`, iOS 16, automatic signing, embedded in the Runner, MSAL through the same SwiftPM package reference as the vendored plugin) sharing the app group `group.com.kammcs.boardhop` and the `com.microsoft.adalcache` keychain group with the Runner. It reads the pointer keys beside `aps`, applies the same guards as Kotlin (not a pointer, stale >10 min, no verb family, no account for the org, no config), acquires the token silently (`brokerAvailability = .none`, `bypassRedirectURIValidation`), runs the same six fetch paths with the same body formats (`EnrichmentFormat.swift` is a function-for-function port), rewrites `body` and, for a file thread, `subtitle`, keeps `title`/`userInfo`/`threadIdentifier`/`categoryIdentifier` intact so the Runner's tap routing still lands on the anchor, and delivers the relay's fallback line on any failure, the 8 s budget or `serviceExtensionTimeWillExpire`. Two things the extension cannot read had to be mirrored into the app group: the MSAL configuration (the vendored `msal_auth` iOS plugin now writes what Dart passed under `MsalAuthConfig` when `Info.plist` names `MsalAuthSharedDefaultsSuite`; `3.5.3+boardhop.2`) and `push.msal.account.{org}` (new `setAccount`/`clearAccount` methods on the push channel, called by `PushRegistrar` on iOS only). Verified: `flutter analyze` clean, 474 Dart tests, 42 XCTest cases in RunnerTests (format, enricher with a stubbed fetcher, pointer contract), a codesigned device build with the appex embedded and both bundles carrying the app group and keychain group, `ExportOptions.plist` needs no change (automatic signing covers the second bundle id). **R2.7 accepted on Kelly's iPhone 16 Pro Max on 2026-09-14** with three approved runs of pipeline 139 (`w29`): run 20260914.2 (`fail=true`) → `buildFailed` pushed, the extension acquired the MSAL token silently and enriched the body in about one second (build + timeline fetch); run 20260914.3 → `approvalPending` enriched (approval fetch) and its tap opened Pipelines → Approvals on the pending card, the approval was granted there, and the following `build.complete` pushed as `buildFixed` and was enriched the same way (breadcrumb: received → token acquired silently → enriched, all inside one second); the Send test push took the fallback path (`nothing to fetch`), as designed. Run 20260914.1's plain success was routed and correctly not pushed (D3). The app-group mirror was verified by pulling `Library/Preferences/group.com.kammcs.boardhop.plist` off the phone with `xcrun devicectl device copy from --domain-type appGroupDataContainer --domain-identifier group.com.kammcs.boardhop`: `MsalAuthConfig` and `push.msal.account.puremedia` were both there after one app start. **Two facts from the device:** (1) the extension's `NSLog` never showed up in `idevicesyslog` (the relay stalls on this iOS 27 phone and the kernel's `EXC_GUARD AST … type=0x1` line is per-process telemetry, not a crash; it fires for every daemon), so the extension now also appends a breadcrumb line per push (`received`, `token: …`, `→ enriched|fallback (reason)`; metadata only, capped at 200 lines) to `Library/Caches/push-extension.log` in the group container, pulled with the same `devicectl` command; that is the way to look at it. (2) After a reinstall the first launch got no APNs token and therefore never registered (the relay saw no `POST /v1/devices`) until a hot restart. Kelly called that a design failure (registration must follow the grant of the permission, not the next start), **fixed the same day**: iOS `refreshToken` now waits up to 30 s for the token like Android's does, the Runner retries `registerForRemoteNotifications` after a failure (5/10/20/40/60 s) and whenever the app becomes active without a token, `PushCoordinator` runs a registration pass on every resume (a heartbeat at most when already registered), and the first token no longer also triggers the rotation path (a pre-existing double `POST /v1/devices` on the first enable). Verified on the iPhone: Settings → Notifications off/on gave `DELETE` then `POST /v1/devices` five seconds later with no restart; `test/features/push_registration_timing_test.dart` covers the wait, the resume and the heartbeat. **Play build validated on the Pixel 10 (2026-09-14, build 4 → 6):** `buildFailed`, `approvalPending` (tap → Approvals card, approved there) and `buildFixed` all enriched by the Kotlin path with the user's own token, ~300 ms per push. **Two feed defects found and fixed on the way** (builds 5 and 6, iOS by hot restart): (1) a pushed approval was announced twice: the Kotlin/extension notification, then the Activity poll's own local notification for the same artifact with the feed row's coarser route (Dart only learned about a pushed pointer when it was in front or the notification was tapped, the R2.6 gap). Now the native poster queues the pointer (Android `PushedQueue` in a private preferences file, iOS `push.pending` in the app group) and Dart drains it via `drainPushed` on the push channel at start, on resume and inside `ActivitySync.run` right before it decides what to notify (`beforeNotify`), inserting each into the feed and marking it notified; one notification per event on both phones, verified with run 20260914.6. (2) The feed never showed the pushed row: `ActivityRepository.refresh` replaced the cache with what it fetched, and builds are only fetched for pinned projects, so the resume poll wiped the row the drain had just inserted. Rows now carry `pushed: true` and `refresh` keeps them for seven days (`withPushed`; a fetched copy of the same key wins). (3) The start-up drain ran before the signed-in accounts were known and dropped the pointer with the platform queue already emptied (iOS, run 20260914.7); the drain now waits for `syncAccounts`, which drains itself. Verified with run 20260914.8: the row appeared in the iPhone's feed on resume with no tap and the app group's `push.pending` emptied. Observation, not a defect: the tethered debug build was once killed by iOS's foreground watchdog (`0x8BADF00D`, scene-update) coming back from the background; the queued pointer survived it. Play builds 4, 5 and 6 went to the internal track for these checks. **Still open:** a real work item or PR push through the extension (only build and approval families were exercised live; the other four are unit-tested against canned JSON), and the enriched body on the lock screen with previews hidden. **Marketplace extension landed 2026-09-13 evening (aaabdfe relay E1 + 36e9b4e extension E2), published private as `KammCS.boardhop` 0.1.0, validated, shared with `puremedia` and `kammcs`:** `extension/` is the customer-side piece from research/06 — a **Boardhop** hub under Organization settings and Project settings (plain TypeScript, host theme variables, `azure-devops-extension-sdk`; the api package is AMD-only so REST goes over `fetch` at 7.1) where an administrator enters the relay URL (stored in the Extension Data Service, collection scope, document `boardhop/settings`, so the app can discover it later) and the **organization key** (the org's hook secret, the plaintext in `relay/.hooks-secret-<org>`; never stored by the extension), checks the connection, and per project **Enable / Repair / Remove** the 14 subscriptions (`src/plan.ts` ports `hooks_lib.dart` and `hook_kind.dart`; 28 node tests) and registers them with the relay through the new **org-key routes** `GET /v1/orgs/{org}/subscriptions`, `PUT|DELETE /v1/orgs/{org}/projects/{projectId}/subscriptions` (Basic `hook:key`, the ingest 404 wall, per-project replace; CORS for `*.vsassets.io`, `dev.azure.com`, `*.visualstudio.com`, localhost only; 349 relay tests; deployed and smoke-tested live with the puremedia key). Scopes `vso.project`, `vso.hooks_write`, `vso.extension.data_write`. **First install verified 2026-09-14 05:48 (UTC-4) in `dev.azure.com/kammcs` (Kelly's own org, backed by a personal Microsoft account):** the hub loaded under Organization settings, Check connection answered through the CORS preflight, Enable on "BoardHop Scratch Project" created 14 subscriptions in Azure DevOps and the relay registered 14 (`hook project subscriptions replaced … 14/14`), Refresh re-read both. A kammcs key exists (`relay/.hooks-secret-kammcs`). **Deliveries verified minutes later:** Kelly created work item 1 there and assigned it to themself; the relay accepted both posts under the kammcs key (200, ~125 ms) and routed `wi.created` (0 candidates: no assignee) and `wi.updated` (1 candidate, 0 recipients: actor = assignee, and no device is registered for kammcs). The only thing kammcs cannot show is a push, because the app cannot sign into a personal-account org (Entra-only, settled). **Tomorrow with the puremedia admin (runbook in `extension/README.md`):** (1) their PCA opens Organization settings → Extensions → Shared → Boardhop → Get it free → Install (grants the three scopes); (2) Organization settings → Boardhop; relay URL stays `https://boardhop.relay.kammcs.com`; Kelly hands over the puremedia key (`relay/.hooks-secret-puremedia`, already set on the relay) over a safe channel; Check connection should say "Connected · 14 subscriptions" (the scratch project's); (3) Enable the projects they agree to — a project administrator of each is needed, a non-admin row says so and the run continues; (4) verify 14/14, `enabled`, Relay 14. If the hub fails to load at all, the fallback is `relay/tool/hooks.dart` with `HOOKS_ALLOW_PROJECT` widened by Kelly for the agreed project. To test-install tonight: the same steps in `dev.azure.com/kammcs` if Kelly is its PCA (a kammcs org key would first need `dart run tool/hooks.dart secret --org kammcs`). **Next:** the puremedia install on 2026-09-14 and fixes from it, a first real delivery from kammcs, then A2/B4/B5 and the app reading the relay URL from the extension data store. Remaining spikes in research/06 §Spikes: extension data store read, `vso.hooks_write` across projects, Minimal payload capture against the scratch project, FCM and APNs end to end from the Hetzner box, private publish and share, SignalR feed subscription for live logs (and whose identity it uses). **Phase R1 landed 2026-09-13**: the relay now registers devices against the user's own Azure DevOps token (`POST|DELETE /v1/devices`, heartbeat, per-org rate limit and kill switch, `GET /v1/admin/devices`) and holds a pointer-only push gateway (FCM live and verified end to end on the Pixel emulator; **iOS push now runs end to end** (2026-09-13, Kelly's iPhone 16 Pro Max, iOS 27.0, Xcode 27.0): APNs token from Apple over the `com.kammcs.boardhop/push` channel, device registered with the relay as `platform: ios`, a real push delivered, and a tap on the banner routing to the activity feed. Three defects were found and fixed on the device, none of which a simulator could have shown. (1) A tapped push had no route home at all: `UNUserNotificationCenter` has one delegate and only its holder sees taps, so a proxy now answers for remote notifications. (2) **`FLTFirebaseMessagingPlugin` takes that delegate when it registers**, even though Boardhop never calls `Firebase.initializeApp()` on iOS, and it keeps whatever it displaced as its own forwarding target — so wrapping it made the two forward to each other and a local notification went round in a circle, never presented. The proxy now hands unclaimed notifications to the **app delegate**, which fans them out to every plugin registered with `addApplicationDelegate:` and cannot come back. (3) The proxy first suppressed the system banner for a foreground push and had Dart re-raise it, copying Android, where FCM shows nothing while the app is open. iOS does show remote pushes, so that only made the phone silent once the replacement could not present; iOS now presents its own and `PushCoordinator._showForeground` is Android-only. The proxy also re-asserts itself on `applicationDidBecomeActive` and on `register`, because the delegate slot is contested and the order is not ours to control), with the app side in `lib/features/notifications/` and a **Send test** row on the Activity feed. **Kelly's work items (2026-09-13), in dependency order; each unblocks the agent work named:**
    - ~~A1 Marketplace publisher~~ **done 2026-09-13**: publisher `KammCS` (Kamm Creative Solutions) exists, the PAT is in `.env` as `BOARDHOP_MARKETPLACE_PAT` and authenticates against the gallery API (publisher read 200, proper 404 for a missing extension); the Manage scope is proven at the first private publish (spike 5).
    - A2 puremedia (meeting 2026-09-14; the private extension is published and shared with `puremedia`, see item 19): written OK from CloudCover that hook metadata lands on the kammcs server; name their PCA who will install the private extension and a project administrator for the CloudCover hooks; ask whether they will provision a service-account PAT for the live-log SignalR subscription → unblocks the beta install and spike 6's identity choice.
    - ~~A3 Scratch hooks~~ **done 2026-09-13**: spike w21 created, read, and deleted a `build.complete` webhook subscription scoped to the scratch project with the spike PAT (the AzureDevOps MCP entry in `~/.claude.json`, kkamm@cloudcover.it), so it carries `vso.hooks_write`; payload capture (spike 3) can start once the capture endpoint exists (B1).
    - ~~B1 Server~~ **done 2026-09-13**: a dedicated Hetzner CX23 `boardhop-relay-1` (Ubuntu 24.04, own Hetzner project and cloud firewall, backups on) at **boardhop.relay.kammcs.com** (A 2.28.109.139 + AAAA), deploy user with Kelly's key, root and passwords off, ufw 22/80/443, unattended upgrades, 2 GB swap, Docker 29 + Compose v5.5, `/srv/relay/{caddy,data,secrets}` (secrets 700). Caddy and the relay run from `/srv/relay/compose.yml`. **The relay skeleton and the capture endpoint are live (2026-09-13):** `https://boardhop.relay.kammcs.com/healthz` answers over a Let's Encrypt certificate and the spike-3 capture endpoint `POST /capture/scratch` (HTTP basic, user `hook`, secret in the box-only `/srv/relay/relay.env`) is up, with the source in `relay/` and the box notes in `relay/README.md`; spike `research/spikes/w22_hook_capture.py` is ready to run in `create` mode to point the scratch project's hooks at it (exact event ids confirmed by spike s39).
    - B2 Apple push: **DONE 2026-09-13.** Key id **9L94ZN33Y3**, key at `/srv/relay/secrets/apns-authkey.p8` on the relay box (not in the repo), `APNS_TEAM_ID=73W98CESN9`, `APNS_ENV=production` (sandbox until 2026-09-14); `/healthz` reports `apns: ready (sandbox)` and a real push was delivered to Kelly's iPhone 16 Pro Max. The Push Notifications capability **is** enabled on the `com.kammcs.boardhop` App ID: automatic signing accepted the `aps-environment` entitlement on the first device build. Two traps cost most of that session and are written up in `relay/README.md`: compose reads `env_file` only when it **creates** a container, so `docker compose restart` silently keeps the old environment and `up -d --force-recreate` is the command; and `/healthz` says `ready` as soon as a key file, key id and team id are present, which says nothing about whether Apple will accept them — the first key tried was a valid `.p8` for the right team and still gave `403 InvalidProviderToken` because it had not been enabled for APNs. B3 Google push~~ **done 2026-09-13**: Firebase project `boardhop-d4b8f` with Analytics off, Android app `com.kammcs.boardhop`, `android/app/google-services.json` in place and gitignored (no SHA-1 registered yet; FCM does not need one), FCM service account at `/srv/relay/secrets/fcm-service-account.json` on the relay box (mode 600). No iOS app in Firebase: iPhones go through APNs directly.
    - B4 Devices: a physical iPhone signed into TestFlight (APNs does not reach the simulator) and a puremedia tester willing to install the beta → unblocks the iOS end-to-end test and the beta itself.
    - B5 Decisions: relay stack (Dart shelf so it shares the app's models, or whatever the Hetzner box already runs), the beta event set (PR reviewer added / updated, work item assigned / commented, build completed, approval pending), and whether live logs are in the beta or follow → unblocks the relay skeleton.
    Intune plugin only if a customer requires it (research/08 §4).

20. **Search across Azure DevOps content (planned with Kelly 2026-09-14, `research/15-search.md`).** Decisions D1–D10: current project by default with an All projects switch; work items, code and pull requests in v1 (wiki waits for a reader, pipelines and repos out); a search icon on the project Home app bar opening a full-screen view; Type and State chips from the facets; last 10 recents per account on the device; title plus one highlighted line; PRs matched locally over the active org list; search as you type from 3 characters with a 400 ms debounce; grouped sections with See all; an inline filter field on the Work tab. Spike s44 (read-only) pinned the Search API shapes (lower-case field names, `<highlighthit>` markup in `hits`, facets, `$orderBy`, org-wide code search). **Built 2026-09-14 in dispatcher mode, three Opus phases, all committed:** S1 data layer (`SearchRepository`, models, recents, `PlainText`; 64 tests), S2 UI (`SearchPage` in the shell's Home branch with the field and the Project | All pill in the app bar, grouped sections with See all, chips and sort in the work item See-all, recents, the Home magnifier and the Work tab's local filter; 34 tests), S3 acceptance on the iPhone 17 and iPad Pro 13" simulators (`research/walkthroughs/2026-09-14-search.md`: every §5 item passed on both, both themes, xxxL, paging past 50 against the live service; two fixes). **Open:** (a) a once-seen frame on the iPad that stopped at the first section, not reproduced after every list child got a key; (b) offline not exercised on a device (this Mac is on wired Ethernet), covered by widget tests; (c) an unknown custom work item type draws the hollow circle, and a PR row in All-projects scope repeats a project whose repository shares its name (`PullRequestTile`, wider than search); (d) the scratch repository answers code search with `infoCode 15` (not indexed), which is the service. **Related shell fix the walkthrough surfaced (Kelly's iPad screenshot):** with the search field focused the glass shell's Scaffold resized for the keyboard inset and lifted the whole stack, dock included, a third of the way up the screen; `GlassShellLayout` now sets `resizeToAvoidBottomInset: false` in both orientations so the keyboard covers the dock as it covers Apple's tab bar, with two tests; **Kelly to confirm in the DeviceHub window with the software keyboard shown** (the headless simulator here reports no inset and DeviceHub's ⌘K did not toggle it). No store upload: Kelly is batching more items into the next shipped version. **Android verified 2026-09-14 evening** (`research/walkthroughs/2026-09-14-android.md`, Pixel 10 Pro and Pixel Tablet emulators, debug build): grouped view, both scopes, See all, a hit opening its detail and Back keeping the results, the Work tab filter, the tablet layout, light theme. One observation: the See-all page showed no Type/State chips on a cached first open.

21. **Mentions in comments (planned with Kelly 2026-09-14, `research/16-mentions.md`; built and accepted the same day).** Decisions M1–M15: `@` people (project scope, people only), `#` work items and `!` pull requests in the work item Discussion composer, the PR Comments composer, thread replies and diff-line threads; a floating list above (or below) the field; styled editable runs; a quiet hint for a typed `@word`; existing mentions restyled on read, artifacts tappable; the existing Graph pair for lookup; arrows/Enter/Escape on the iPad. Spikes s45–s48 and w30 settled the wire format (`@<identityGuid>` on the markdown routes, `mentions[]` stamped by the service; the relay already routes it). Landed in four phases (M-A data/text layer, M-B `MentionField`, M-C hosts and read side, M-D acceptance): 173 new tests, walkthrough `research/walkthroughs/2026-09-14-mentions.md`, proven on scratch #15545 (comments 6068143, 6068287 read back with `mentions[]` = 1) and PR 8334 (thread 42566 reply, thread 42678). Side fixes: the glass shell keeps in-shell composers above the keyboard (93808cd had hidden them); `Routes.workItemStandalone` for pushes that start over the shell (the PR page's linked work items asserted before); iOS Return picks the highlighted row. **Open:** the relay never pushes a self-mention (actor exclusion), so "mentioned you" on a phone needs a second person in the scratch project; the app's Entra token has not been checked against `vssps identities?identityIds=` (PAT works; names are seeded from the PR's people so it rarely runs); Android/Gboard composing untested (this Mac's debug redirect URI is not on the app registration and there is no `boardhop_pixel_10_pro` AVD here, only `Pixel_10_Pro`); offline queueing covered by widget tests only (Ethernet Mac); mention runs wear `BoardhopColors.mention` (link blue, semibold) in both themes since Kelly asked for them to be very obvious. **Android/Gboard verified 2026-09-14 evening** (same walkthrough): `@`, `#` and `!` lists over Gboard with predictions on, picks styled, posted from the work item and PR composers, read back tappable; drag-to-dismiss keeps the text. Later phases by decision: HTML-field editor mentions (needs a Summernote-side picker spike), PR description editing, groups, tapping a person, the `IdentityPicker` endpoint probe. No store build until Kelly asks. **Keyboard gap (2026-09-14):** the comment lists dismiss the keyboard when a drag starts (`ScrollViewKeyboardDismissBehavior.onDrag`), not iOS's interactive drag that follows the finger down over the keyboard (`UIScrollView.keyboardDismissMode = .interactive`); Flutter does not expose that, so a more native feel would need a platform view or engine-side hook. Look into it if Kelly wants it.
22. **Work item detail in three tabs (Kelly's ask, 2026-09-14; built the same day).** "Split work items into three tabs — the detail, related, and comments. Related and comments can have numbered badges. This breaks up longer work items into something more easily digestible." `WorkItemDetailPage` now owns a `TabController` the way the pull request page does: **Details** (header, facts, the type's field sections), **Related** (the Links section plus a read-only **Attachments** list — name, size and thumbnail, opened through the form's own `openAttachment`, extracted out of `AttachmentsSection` so the byte fetch lives in one place; `AttachmentSource.upload` is now nullable for the read-only host) and **Comments** (the Discussion, with `CommentComposer` as the scaffold's `bottomNavigationBar` on that tab alone). Badges come from `lib/features/shared/widgets/tab_count_badge.dart` (`TabCountBadge`, `TabCount`, `CountedTabBar`), shared so the PR page can adopt them later; a count of zero or an unknown count draws nothing. The strip divides the width evenly while the labels and their pills fit and **scrolls when they do not** — measured with a `TextPainter` rather than the PR page's 1.3× text-scale guess, because "Comments 14" faded its last letter on the iPhone at the ordinary size (label padding is also 8 a side rather than Material's 16). `?tab=details|related|comments` on both work item routes (`Routes.workItem`, `Routes.workItemStandalone`, router, `initialTab`); a `?comment=` push selects Comments first and scrolls after that tab has built. The selected tab survives a `watchItem` rebuild, a refresh and the edit form; switching tabs refetches nothing; each tab has its own `RefreshIndicator`. The embedded tablet pane keeps the strip in its own app bar. **Verified on the simulators** (iPhone 17 and iPad Pro 13", debug): #15545 all three tabs light and dark and at xxxL (the strip scrolls, nothing clipped), #15546 links with a "2" badge, #15553 links and two image attachments with a "4" badge and the full-screen viewer opening from a row, and the iPad list+detail pane with the composer only on Comments. 12 new tests (`test/features/work_item_detail_tabs_test.dart`, plus the routes and router cases); the comment-anchor test now asserts the tab switch. **Android verified 2026-09-14 evening** (same walkthrough): badges, the Comments-only composer, the tablet pane strip. **Open:** the Details facts rows still repeat an "Attachments" count that the Related badge now also carries; the "Discussion (N)" heading is gone because the tab label and badge say it; the PR page was left on its own strip on purpose.

23. **Images and files in comments (planned with Kelly 2026-09-14, `research/17-attachments.md`).** Decisions T1–T10: attach from all three comment composers (any file; upload on Send; comment-only, no relation; camera photos shrunk to 2048/85, library and files byte-exact; online-only; several per comment; markup appended as a trailing block; inline images with tap-to-view, file rows through the share sheet; Gboard rich-content pass-through; no clipboard package). Spikes w32/s50 settled the wire shapes and found two live read bugs (the `\x06` image prefix in `html`-format work item comments; markdown images invisible in PR comments and descriptions). Landed the same day in three phases (T-A read side and the PR upload call, T-B composers, T-C acceptance): 100 new tests (suite 885), walkthrough `research/walkthroughs/2026-09-14-attachments.md`, proven on scratch with the app's own Entra token (#15545 comments 6068641 and 6068650; PR 8334 threads 42511, 42727 and 42739; the PR store went from 4 to 6 rows with no name collision). Side fixes: the two read bugs, a 320 pt cap on comment images on the HTML path, `openAttachment` reports the service's message. **Open:** after a thread reply posts on the PR page, focus lands on the page composer so the keyboard stays up (not attachment-specific); a PR attachment opens under its stamped name rather than the picked label; offline (T6) is covered by widget tests only (the Mac's Ethernet cannot be toggled from a session); Gboard and the camera path unverified on a device (this Mac's Android debug redirect URI is not on the app registration); the web's own rendering of app-posted attachments is corpus evidence only. **Android verified 2026-09-14 evening** (same walkthrough): the Android 16 photo picker (work item comment, 1.2 MB, badge 19 → 20) and the **camera path** (PR comment, shrunk to 34 KB, new thread with the image), inline images and file links on read, the viewer. Shipped as build 12 on 2026-09-15.

24. **Layout goal for the next hubs (Kelly, 2026-09-15): mirror the web's hubs and keep the dock at four.** Azure DevOps groups Overview (Summary, Dashboards, Wiki), Boards (Work items, Boards, Backlogs, Sprints), Repos and Pipelines; the dock is Home · Work · Repos · Pipelines and each tab already owns a segmented pill as its rightmost app-bar item. So: the **Work** tab's pill becomes Items | Board | Sprint (the sprint taskboard is a board with an iteration picker to the left of the pill, current sprint by default); the **Home** tab becomes the Overview hub with the pill Summary | Dashboards | Wiki (Summary is today's Home; Wiki lands on its page tree and pushes pages over the shell). No fifth tab: the dock stays uncrowded on phones and identical on the tablet rail, the pill already goes icon-only on phones, and the grouping matches the mental model every Azure DevOps user has. Cautions recorded for the later interviews: dashboards are widget canvases and a faithful render is a large job (a first pass is more likely a few Analytics OData charts: burndown, cycle time, work by state); three-segment pills are icon-only on phones, so summary, dashboard and wiki need unmistakable icons. **Sprint support is next** (research, interview, then build); dashboards/analytics and wiki after it.

25. **Sprints (planned with Kelly 2026-09-15, `research/18-sprints.md`).** Decisions S1–S12: the third segment of the Work pill; an in-page tab strip Backlog | Taskboard | Burndown (Taskboard when the sprint has task-type items, else Backlog); writes = move a task (drag and a tap-to-move sheet), Remaining Work and assign to me, add a task under a story, move an item into or out of the sprint; phone = one Kanban board with a story chip strip, tablet = a rows × columns grid with a supporting pane; burndown from Analytics (count + story points) through `fl_chart`, verdict and sparkline in the header, built after an on-device token check; capacity read-only when set; default team with a switch in the picker sheet; offline-first snapshot (and the Kanban board fixed to read its cache); person filter with Me; no sprint admin; an ended sprint shows "Ended N days ago". Spikes s54/s55, w33–w35 (the scratch taskboard is now customized To Do / In Progress / Verify / Done and Iterations 1–2 carry dates). Landed the same day in four phases (P-A data layer and the board's offline fix, P-B widgets, P-C the page and routing, P-D acceptance): 264 new tests (suite 1085), walkthrough `research/walkthroughs/2026-09-15-sprints.md`, `fl_chart` 1.2.0 pinned, the app's Entra token accepted by the Analytics host on the simulator. S13 (Kelly, on real data): no header or chart on the Taskboard tab, the tablet grid takes the full width. Device findings: the Agile/Scrum processes empty Remaining Work on Done and refuse a written zero (moves stay state-only with a fallback patch); the ideal line is anchored on the sprint's finish; the verdict compares the Analytics series against its own ideal; the header ignores the person filter; `WorkItemCard` no longer overflows at xxxL with a badge. **Open:** the person filter narrows tasks but not Backlog rows (decide whether rows without a matching task should hide); the hours-vs-items unit rule is "any task has hours"; the sprint's `+` uses the first task type any column maps rather than the type chooser; a real drag and airplane-mode offline are test-only on the simulator; the Work items page has the same crushed two-line phone title the sprint page just lost; every puremedia project has one team so the team switch is unexercised on a device. Shipped as build 13 on 2026-09-15.


## Conventions

- `FilledButton` minimum size is 48×48 in the theme; never `Size.fromHeight`, which is an infinite width and breaks buttons inside a `Row`. Full-width CTAs stretch through a `Column(crossAxisAlignment: stretch)` or a `ListView`.
- Design rules live in DESIGN.md: one master theme in `lib/theme/`, light and dark both first class, colors only through `Theme.of(context)` and `context.boardhopColors`, spacing and radii from tokens, every screen checked at compact and expanded width.

- Never commit client IDs, tokens or raw spike results. `.gitignore` already excludes `research/spikes/results/*.md` except the README, plus `.env` and `android/secret.properties`.
- After changing drift tables run `dart run build_runner build`; the generated `app_database.g.dart` is committed.
- Spikes against puremedia: writes only in the "DevOps Mobile App" project. Run with `python research/spikes/_run_with_mcp_creds.py <script>`.
- Commit messages end with `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`.
- Azure DevOps API: pin `api-version=7.1` per operation; read work items without a `fields` filter when the format map is needed; always send `test /rev`.

26. **Dashboards (planned with Kelly 2026-09-15, `research/19-dashboards.md`).** The first Home hub from item 24: the Home pill becomes Summary | Dashboards (Wiki slots in as the third segment later). Decisions D1–D15: render the team's real Azure DevOps dashboards plus a Boardhop-built Team overview (six Analytics/REST cards) offered when a dashboard is empty; all three widget tiers (query tiles and results, Assigned to Me, PRs, Team Members, Markdown, links, Build History; Burndown/Burnup, Sprint Overview and Sprint Burndown; Cycle/Lead Time, CFD, Velocity); Chart for Work Items (an unreadable `chartId`, no query id anywhere in the widget) is hidden under the footer line (D2 revised after w36); phones stack widgets in reading order, tablets pack the web's spans into 168 dp columns; the picker is the title (all teams, favorites first, last opened remembered) and Search stays on Summary; taps drill into lists and open a full-screen focus view for charts; unsupported widgets are hidden behind a footer line; pull-to-refresh only; native charts, no PNGs; per-card Analytics refusal notice. Spikes s57–s61 (read-only) settled the API (preview versions only, GET by id needs the team segment, settings shapes, no chart API for saved queries, Analytics timings); w36 (scratch write) adds the unseen widget kinds to the scratch Overview as test data. **Landed 2026-09-15** in dispatcher mode over four phases (research/19 §7 has the full account). **D-A** the data layer: models with a tolerant typed settings parser per kind, `DashboardLayout` (the pure D4 grid mapper), `DashboardRepository` in `AccountDeps` (list, get by id with the team segment, favorites, widget-type catalog, all cached and cache-first), eight typed Analytics queries and `DashboardPrefs`. **D-B** the Home pill (`HomeViewSwitch`, icons only on a phone, shaped for Wiki), the `:project/dashboards` route in the Home branch, `DashboardPage` (title-as-picker, cache-first open with an "Updated N min ago" line, pull-to-refresh through `DashboardReloadScope`, the D1 Team-overview offer, the D10 footer line), the picker sheet, the card frame with its four states, and ten tier-1 cards; `WorkItemListTile` was lifted out of `work_items_page.dart`, `Routes.workItems` gained `query`/`queryName` for D7, and `DashboardWidget.builtInKind` keeps the synthetic Team overview from being classified as a Marketplace widget. Diagnostics gained `/diagnostics/dashboard` and the "Dashboard API" and "Analytics widget sets" checks — the first of which found the token refused (`401 TF400813`) for want of `vso.dashboards`, granted and consented the same day (research/09). **D-C** the drawing layer: a sealed `ChartPayload` per chart so a card and the focus view draw the same thing, `VelocityChart`, `CumulativeFlowChart`, `CycleTimeChart`, `WorkStateBars`, `RunOutcomeBars`, seven chart cards, `ChartFocusPage` on `:project/chart/:widget` outside the shell (D7/D15), the `chartSeries` ramp and `axisTextScale`. **D-D acceptance: verified on the iPhone 17 and iPad Pro 13" simulators** (debug builds, light and dark, xxxL, portrait and landscape) on the scratch project and read-only on the client projects. The `vso.dashboards` grant works — the token was accepted by the Dashboard API on the first call with no fresh sign-in. All 13 w36 widgets parse; 12 render and Chart for Work Items is hidden behind "1 widget not shown in Boardhop"; every D7 tap drills where it should; the Team overview draws all six cards; the focus view follows the orientation; the last-opened dashboard survives a cold restart and `?dashboard=` overrides it; a client project's empty Overview offers the Team overview and its populated Overview renders 8 of 10 widgets with the two Chart for Work Items hidden behind the footer count. Five device defects fixed: **the Burndown and Burnup widgets were a 400 on any real dashboard** (the settings carry the work item reference name `Microsoft.VSTS.Scheduling.StoryPoints` and Analytics wants its own `StoryPoints` — `WidgetAggregation.analyticsField` now maps between them; the scratch project could never have shown this, because w36's Burndown stores no aggregation), and four chart cosmetics with one cause — the axis maximum was `dataMax × 1.1`, so the axis printed the headroom value (9.9 for nine items, 18.7 for seventeen, 81.1 on the client velocity) and half of that label, centred on the plot area's top edge, landed in the card's caption. `chartAxis` in `lib/theme/layout.dart` now rounds the maximum and returns the interval with it, `showsLabel` drops the top label against half an interval, `label` formats with exactly the precision the interval needs (0.25 was printing as "0.3"), and `chartInsets` leaves the last x label room for its own half ("15 Sep" was clipped to "15 Se"); the four charts including `SprintBurndownChart`, shared with the Sprint page, use it. Feature tests run 191, whole suite 1313 green. Walkthrough: `research/walkthroughs/2026-09-15-dashboards.md`. **Open:** favorites-first ordering in the picker is test-only (nothing in puremedia is favorited, and every project has one team with one dashboard); `project`-scoped dashboards have still never been seen anywhere; the D14 Analytics-refused notice stays test-only because the host accepted every call; a chart card opens its focus view from its title, not its plot area, because fl_chart owns the touch inside the chart; the packed grid leaves half-empty rows on a dashboard whose spans were never laid out for two columns (D4 as specified, but it reads sparse); offline stays covered by widget tests; `tool/shot-ios.sh` double-rotates landscape thumbnails on iOS 26.5, where `simctl` already returns the rotated frame. **No store upload until Kelly asks.** Android is untested on this Mac: its debug redirect URI is not on the app registration.
