name: boardhop_relay
description: Boardhop push relay — HTTPS receiver for Azure DevOps service hooks.
publish_to: none
version: 0.0.1

environment:
  sdk: ^3.13.0

# Exact versions: the container build must be reproducible.
dependencies:
  shelf: 1.4.2
  shelf_router: 1.1.4
  sqlite3: 2.9.0
  crypto: 3.0.6
  logging: 1.3.0

dev_dependencies:
  lints: 6.0.0
  test: 1.26.2
