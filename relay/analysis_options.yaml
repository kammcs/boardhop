include: package:lints/recommended.yaml

# The app is formatted at 120 columns; the relay follows.
formatter:
  page_width: 120

analyzer:
  language:
    strict-casts: true
    strict-raw-types: true
